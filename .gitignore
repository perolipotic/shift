node_modules/
dist/
coverage/

# Vite / build caches
.vite/
*.tsbuildinfo

# Local environment — the publishable key is injected at build time, the secret
# key lives only in the Edge Function's per-environment env (AD-17).
.env
.env.*
!.env.example

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

.DS_Store
*.log

# Playwright E2E: stored sessions and per-run fixture facts, and run output
e2e/.auth/
test-results/
playwright-report/
# the remote smoke's own (playwright.smoke.config.ts)
test-results-smoke/
playwright-report-smoke/
blob-report/
