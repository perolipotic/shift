# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed

# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created, ready for development
#   - in-progress: Developer actively working on implementation
#   - review: Implementation complete, ready for review
#   - done: Story completed

# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed

# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed

# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when its first story starts (via build's sprint sync)
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates the next story after the previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; the status view surfaces open ones
generated: 09-02-2026 14:26
last_updated: 09-25-2026
project: shift
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts
development_status:
  epic-1: in-progress
  # Story 1.1 was split twice during build. The parent key below stays
  # in-progress until every part lands; the parts carry their own status.
  # These four keys are not derived from epics.md, so a regenerate reports
  # them as orphans — reconcile with --set rather than dropping them.
  # The navigation shell is NOT one of the parts: it was added to 1.1d and then
  # split back out at 1.1d's token gate, and it appears in no epics.md
  # acceptance criterion for story 1.1. It waits in deferred-work.md and earns
  # its own key when specced, so the parent below completes with 1.1d.
  # Completed 2026-09-15, per the convention recorded above: all four parts
  # (1.1a-1.1d) are done and the navigation shell is explicitly not one of them,
  # so the parent carries no remaining work.
  1-1-a-deployable-shell-that-speaks-croatian-in-both-themes: done
  1-1a-deployable-shell: done
  1-1b-theme-layer: done
  1-1c-localization-layer: done
  1-1d-croatian-sign-in-shell: done
  # 1.2 was stale at `review`: spec-1-2-organization-provisioning.md carries
  # status done with review_loop_iteration 1, and its findings were closed in
  # 68df522/a1416a3. Reconciled 2026-09-07.
  1-2-an-operator-provisions-an-organization-that-is-born-with-an: done
  # Story 1.3 was split once at the planning token gate (~6,300-9,749 tokens
  # against the 900-1600 standard). Same convention as 1.1: the parent key
  # below stays in-progress until every part lands; the parts carry their own
  # status, are not derived from epics.md, and a regenerate reports them as
  # orphans — reconcile with --set rather than dropping them.
  # 1.3a is the access-control layer (epics.md acceptance clauses 2, 3 and 4:
  # isolation, role refusal, and fresh role/active state). 1.3b is the SPA
  # sign-in path (clause 1) and waits in deferred-work.md, earning its key
  # when specced — so the parent completes with 1.3b, not with 1.3a.
  # Completed 2026-09-08 with 1.3b, per the convention recorded above: the
  # parent carries no work of its own and closes when its last part lands.
  1-3-signing-in-reaches-exactly-one-organization-at-exactly-one-r: done
  1-3a-access-control-layer: done
  # 1.3b is the SPA sign-in path (epics.md acceptance clause 1). Specced
  # 2026-09-07 from the deferred-work entry 1.3a left; earns its key here,
  # same convention as the 1.1 parts. The 1-3 parent completes with it.
  1-3b-spa-sign-in-path: done
  # The navigation shell, specced 2026-09-08 — it earns its key here, exactly as
  # the comment above story 1.1's parent anticipated. It derives from no epics.md
  # acceptance criterion (story 1.1's ACs mention navigation nowhere), so it
  # carries no story number and a regenerate will report it as an orphan —
  # reconcile with --set rather than dropping it, same as the 1.1 and 1.3 parts.
  # Split once at the planning token gate: part A is the route skeleton and the
  # vocabulary-gate migration; part B is the chrome and sign-out, which waits in
  # deferred-work.md and earns its own key when specced. Because part A ships no
  # sign-out it does NOT earn the reserved word `odjava`, which stays banned.
  # Was stale at `review`: spec-navigation-shell-a-route-skeleton.md carries
  # status done with review_loop_iteration 0, and the work merged to main in
  # b777832 (PR #4). Reconciled 2026-09-15. Part B is still unspecced and earns
  # its own key when specced, same convention as the parts above.
  navigation-shell-a-route-skeleton: done
  # Part B is the chrome and the exit — the tab bar, the sidebar, destination
  # icons, the non-colour active treatment, `signOut`, and the already-signed-in
  # redirect on both sign-in routes. Specced 2026-09-16; earns its key here,
  # same convention as the parts above, and a regenerate reports it as an orphan
  # because it derives from no epics.md acceptance criterion — reconcile with
  # --set rather than dropping it. It earns the reserved word `Odjava`, which
  # part A deliberately did not: the shipped literal is the imperative
  # `Odjavi se`, so `RESERVED_STEMS` empties and the two blocks it guarded are
  # rewritten in the same commit. It also introduces the first client-side read
  # of `members`, because `destinationsFor` has had no role to filter on since
  # part A shipped it.
  # Part B merged to main in 807cb36 (PR #9) and its spec carries status done
  # with review_loop_iteration 0 — three adversarial layers, twenty-two findings
  # patched, two deferred, no loopback. Two of them were demonstrated rather
  # than argued: the layout could be reverted to a bare outlet, deleting the
  # whole deliverable, with 1055 tests green. Reconciled 2026-09-16.
  # The navigation shell is complete: part A the routes, part B the chrome and
  # the exit. `Odjava` is earned and no longer reserved.
  navigation-shell-b-chrome-and-sign-out: done
  # Story 1.4 was split once at step-01's multi-goal check (human decision
  # 2026-09-15). Same convention as 1.1 and 1.3: the parent key below stays
  # in-progress until every part lands; the parts carry their own status, are
  # not derived from epics.md, and a regenerate reports them as orphans —
  # reconcile with --set rather than dropping them.
  # 1.4a is the identity and localization settings surface (epics.md acceptance
  # clauses 1 and 2: the organization timezone frames every render, and
  # Organization Type is inert). Part B is the branding half — logo, storage
  # bucket, brand accent (clauses 3 and 4) — which waits in deferred-work.md and
  # earns its key when specced, so the parent completes with part B, not 1.4a.
  # COMPLETE 2026-09-17 with part C, per the convention recorded above: the
  # parent carries no work of its own and closes when its last part lands.
  # Its four epics.md acceptance clauses are covered by 1.4a (clauses 1 and 2 —
  # the timezone frames every render, Organization Type is inert), 1.4b
  # (clause 3 — the asset readable only within its owning organization, and the
  # neutral fallback) and 1.4c (clause 4 — the accent tints the shell and the
  # logo lockup and stops there, with `destructive` still reserved).
  1-4-an-admin-configures-the-organization-s-identity-localization: done
  # Part A was stale at `review`: spec-1-4a-organization-settings.md carries
  # status done with review_loop_iteration 0 (implemented, three adversarial
  # layers run, nine findings patched), and the work merged to main in 7076d55
  # (PR #6). Reconciled 2026-09-16, same as the 1.2 and navigation-shell-A
  # reconciles above. The 1-4 parent deliberately stays in-progress — per the
  # convention recorded there it completes with its last part, not with part A.
  1-4a-organization-settings: done
  # Part B is the logo, specced 2026-09-16 — it earns its key here, same
  # convention as the 1.1, 1.3 and navigation-shell parts, and a regenerate
  # will report it as an orphan because it derives from no epics.md key of its
  # own: reconcile with --set rather than dropping it. It covers epics.md
  # story 1.4 acceptance clause 3 (the asset readable only within its owning
  # organization, and the neutral fallback).
  # Split once at step-02's token gate (human decision 2026-09-16): the brand
  # accent became part C and waits in deferred-work.md, earning its own key
  # when specced. So the 1-4 parent above no longer completes with part B —
  # it now closes with part C, amending the note recorded there.
  # Part B merged to main in f14dd2f (PR #7) and its spec carries status done
  # with review_loop_iteration 0 — three adversarial layers run, seventeen
  # findings patched, two deferred, no loopback. Reconciled 2026-09-16 in the
  # same session that built it, so this key never went stale the way 1.2, the
  # navigation shell and 1.4a each did.
  1-4b-organization-logo: done
  # Part C is the brand accent AND the logo lockup, specced 2026-09-17 — human
  # decision that day to cover both, so UX-DR5 ("the accent tints the
  # application shell and the logo lockup only") becomes literally true rather
  # than half-satisfied; the lockup had nowhere to live until the navigation
  # chrome shipped in 807cb36. Earns its key here, same convention as the parts
  # above, and a regenerate reports it as an orphan. Second human decision that
  # day: a curated four-accent palette authored as measured tokens, not a
  # free-form colour — the database stores a key and the stylesheet the colour,
  # and red stays absent because `destructive` owns it (UX-DR4).
  # THIS PART COMPLETES THE 1-4 PARENT, which has been in-progress since 1.4a.
  # Part C merged to main in cc190ff (PR #11) and its spec carries status done
  # with review_loop_iteration 0 — three adversarial layers, twenty-nine findings
  # patched, two deferred, no loopback. Reconciled 2026-09-17.
  1-4c-brand-accent-and-lockup: done
  # The root route's chrome gap, specced 2026-09-17 from the deferred-work entry
  # the navigation shell's part B left. It derives from no epics.md acceptance
  # criterion — `/` appears in none of story 1.1's clauses — so it carries no
  # story number and a regenerate reports it as an orphan: reconcile with --set
  # rather than dropping it, same convention as the navigation-shell parts.
  # `/` stops being a screen and becomes the decision `router.test.ts` already
  # described it as: signed in it forwards to the first destination, signed out
  # it redirects to /prijava as before. Verified in a browser first.
  # Merged to main in 3d46372 (PR #13) and its spec carries status done with
  # review_loop_iteration 0 — three adversarial layers, thirteen findings
  # patched, three rejected, no loopback. Reconciled 2026-09-17.
  root-route-redirect: done
  # Story 1.5 was split once at step-01's multi-goal check (human decision
  # 2026-09-17). Same convention as 1.1, 1.3 and 1.4: the parent key below stays
  # in-progress until every part lands; the parts carry their own status, are
  # not derived from epics.md, and a regenerate reports them as orphans —
  # reconcile with --set rather than dropping them.
  # 1.5a is the read path — the member list at scale (epics.md acceptance
  # clauses 3 and 4: the list stays usable at several hundred members, the table
  # scrolls in its own container with no horizontal page scroll, and a member
  # record carries only name, optional email, team, hours and leave). The write
  # path — createUser/updateUserById in the privileged auth function, the member
  # form, the no-email identity and the admin-issued password reset (clauses 1
  # and 2) — waits in deferred-work.md and earns its key when specced, so the
  # parent completes with it, not with 1.5a.
  # COMPLETE 2026-09-23 with part C, per the convention recorded above: the
  # parent carries no work of its own and closes when its last part lands.
  # Its acceptance clauses are covered by 1.5a (clauses 3 and 4), 1.5b (clause 1's
  # account-without-email and the privileged boundary, and clause 2) and 1.5c
  # (clause 1's admin-issued password reset).
  1-5-an-admin-creates-and-edits-members-and-works-the-list-at-sca: done
  # Part A, specced 2026-09-17 — it earns its key here, same convention as the
  # parts above, and a regenerate will report it as an orphan because it derives
  # from no epics.md key of its own: reconcile with --set rather than dropping
  # it. It is the first role-guarded route in the tree: `members_select_own_organization`
  # deliberately lets a member-role session read the list (0003:286-288), so the
  # admin-only refusal on /ljudi is a route decision rather than a data one, and
  # it changes router.test.ts's pinned session-only assertion — the change
  # _app.tsx:41-52 anticipated for whichever story first gave a destination
  # something to hide. It adds NO dependency: @tanstack/react-table was installed
  # and then removed (human decision 2026-09-17) because the search, sort and
  # filter are pure functions and the markup is shadcn's Table, which left the
  # library imported nowhere. ARCHITECTURE-SPINE.md:218's 9.2.4 pin stands for
  # whichever story first has a use for it.
  # Part A merged to main in 8b122aa (PR #15) and its spec carries status done
  # with review_loop_iteration 1 — the first story in this epic to take a
  # bad_spec LOOPBACK rather than patch through. Round one's three adversarial
  # layers demonstrated rather than argued: reordering MEMBER_ROLES, widening the
  # guard to `|| outcome.ok`, and swapping two headers' sort keys each left the
  # whole suite green while inverting the route guard, admitting every member to
  # the list, and mispairing sorted columns. The code was reverted, the spec
  # amended to require the guard's role reader to arrive through the router
  # context and all screen logic to live in members/list.ts, and re-derived.
  # Round two found three MORE green mutations of the same class and was routed
  # as 25 patches: the cause is that vitest collects only src/**/*.test.ts in a
  # node environment, so a .tsx is executed by nothing and logic there is
  # unverifiable by construction. A detector now fails if ljudi.tsx touches a
  # member field at all, which closes the class rather than the instances.
  # Six previously-green mutations now each fail the suite.
  1-5a-member-list: done
  # Part B is the write path, specced 2026-09-18 from the deferred-work entry
  # 1.5a left. It earns its key here, same convention as the 1.1, 1.3, 1.4 and
  # navigation-shell parts, and a regenerate reports it as an orphan because it
  # derives from no epics.md key of its own — reconcile with --set rather than
  # dropping it. It covers epics.md story 1.5 acceptance clause 1 (an account
  # usable with no email address, created through the single privileged auth
  # function, which uses the secret key for the auth call only and the calling
  # admin's JWT for the `members` row, and refuses unless the caller is an admin
  # of the target's own organization) and clause 2 (per-member leave allowance).
  # Four human decisions of 2026-09-18, NOT to be relitigated: (a) the sign-in
  # username becomes a real `members` column in migration 0007 rather than
  # staying the unreadable local part of `auth.users.email` — the open question
  # deferred-work.md:286 assigned to story 1.5; (b) usernames are editable, so
  # `updateUserById` does real work and only `ban`/`unban` stay at 501; (c) the
  # initial password is generated in the function and shown exactly once, never
  # typed by an admin; (d) the forms are separate routes `/ljudi/novi` and
  # `/ljudi/$id`, vendoring no new primitive — `components/ui/` still has no
  # Dialog. The admin-issued password RESET was split off at step-01's
  # multi-goal check the same day and waits in deferred-work.md, so the `1-5`
  # parent above does NOT close with this part — it closes with the reset.
  # Round one's three adversarial layers found FOUR demonstrated green mutations
  # of one class — assertions comparing a value to itself — and it took a
  # bad_spec LOOPBACK rather than patches, the second story in this epic to do
  # so. `ADMIN_ROLE` set to 'administrator' left 1412/1412 passing while the
  # boundary would have refused every legitimate admin, so no account could ever
  # be issued. Round two, against the re-derived code, found the detached
  # `Response.prototype.json` that collapsed EVERY function refusal to "try
  # again" in the browser — a real production defect hidden because the test
  # stub's `json` was an arrow closure that never touched `this`. That round was
  # routed as 23 patches, not a second loopback: the findings were localised and
  # the structure held. Four rejected as unreachable and recorded so they are
  # not "fixed" later.
  # Merged to main in 63bee27 (PR #17) and its spec carries status done with
  # review_loop_iteration 1 — two review rounds, one bad_spec LOOPBACK, then 23
  # patches, four findings rejected as unreachable and recorded so they are not
  # "fixed" later. Reconciled 2026-09-18 in the same session that built it, so
  # this key never went stale the way 1.2, the navigation shell and 1.4a each
  # did. The 1-5 parent above deliberately stays in-progress: per the convention
  # recorded there it closes with its LAST part, which is the admin-issued
  # password reset waiting in deferred-work.md, not with this one.
  1-5b-member-write-path: done
  # Part C is the admin-issued password reset, specced 2026-09-18 from the
  # deferred-work entry 1.5b left. It earns its key here, same convention as the
  # parts above, and a regenerate reports it as an orphan — reconcile with --set
  # rather than dropping it. It covers the second half of epics.md story 1.5
  # acceptance clause 1 ("its password reset is an admin-issued action rather
  # than a self-service email", CAP-1, AD-12). THIS PART COMPLETES THE 1-5 PARENT.
  # Merged to main in 2213c19 (PR #19) and its spec carries status done with
  # review_loop_iteration 1 — one bad_spec LOOPBACK over the reset's four-stage
  # machine, then three patches from one targeted verification-gap layer.
  # Reconciled 2026-09-23.
  1-5c-admin-issued-password-reset: done
  # Merged to main in 04f8768 (PR #20) and its spec carries status done with
  # review_loop_iteration 1 — one intent_gap LOOPBACK (never-zero-admins read as
  # "active on every date from D onward", and a not-yet-effective version may be
  # cancelled), then 12 patches from the second round; five findings deferred to
  # deferred-work.md. Reconciled 2026-09-23. Story 1.6 was never split, so no
  # parent key waits on it.
  1-6-deactivating-a-member-changes-the-future-and-rewrites-no-his: done
  # Story 1.7 was split once at step-01's multi-goal check (human decision
  # 2026-09-23). Same convention as 1.1, 1.3, 1.4 and 1.5: the parent key below
  # stays in-progress until every part lands; the parts carry their own status,
  # are not derived from epics.md, and a regenerate reports them as orphans —
  # reconcile with --set rather than dropping them.
  # 1.7a is the teams themselves (epics.md acceptance clauses 1 and 4: any count,
  # no name carries meaning, and removal archives rather than deletes). 1.7b is
  # versioned team membership (clauses 2 and 3) and waits in deferred-work.md,
  # earning its key when specced — so the parent completes with 1.7b.
  # Completed 2026-09-25, per the convention recorded above: both parts (1.7a
  # and 1.7b) are done. The member list's team filter split out of 1.7b is not
  # a part of 1.7 and waits in deferred-work.md, so the parent carries no
  # remaining work.
  1-7-an-admin-creates-teams-and-moves-people-between-them: done
  # Merged to main in 988743f (PR #22) and its spec carries status done with
  # review_loop_iteration 0 — three adversarial layers, no loopback, 12 patches,
  # two findings deferred to deferred-work.md (whitespace trimming and name
  # length, both shared with members and organizations). Reconciled 2026-09-24.
  # The 1-7 parent above stays in-progress: per the convention recorded there it
  # closes with its LAST part, which is 1.7b's versioned team membership waiting
  # in deferred-work.md, not with this one.
  1-7a-teams: done
  # Part B is versioned team membership, specced 2026-09-24 from the
  # deferred-work entry 1.7a's split left. It earns its key here, same
  # convention as the parts above, and a regenerate reports it as an orphan —
  # reconcile with --set rather than dropping it. It covers epics.md story 1.7
  # acceptance clauses 2 and 3 (CAP-6, AD-2, UX-DR20). THIS PART COMPLETES THE
  # 1-7 PARENT. The member list's team filter was split out at step-01 (human
  # decision 2026-09-24) and waits in deferred-work.md; it is not a part of 1.7.
  # Merged to main in cfd629f (PR #24) and its spec carries status done with
  # review_loop_iteration 0 — three adversarial layers, no loopback, 6 patches,
  # three findings deferred to deferred-work.md (cross-transaction
  # serialization of membership writes, a renamed team's old name on the member
  # list until its stale time, the unbounded team-history embed). Reconciled
  # 2026-09-25. THIS PART COMPLETED THE 1-7 PARENT above.
  1-7b-team-membership: done
  1-8-a-member-sees-who-is-on-a-team: backlog
  epic-1-retrospective: optional

  epic-2: backlog
  2-1-an-admin-defines-the-day-s-hour-bands-so-a-gap-cannot-be-exp: backlog
  2-2-an-admin-defines-shift-types-including-one-that-crosses-midn: backlog
  2-3-an-admin-builds-the-rotation-and-sees-the-cycle-before-savin: backlog
  2-4-rotation-configuration-completes-on-a-phone: backlog
  2-5-saving-a-rotation-reports-what-it-will-actually-do: backlog
  2-6-an-admin-changes-the-rotation-from-a-date-forward: backlog
  epic-2-retrospective: optional

  epic-3: backlog
  3-1-anyone-reads-a-month: backlog
  3-2-the-month-is-readable-on-a-phone-and-by-a-screen-reader: backlog
  3-3-filtering-to-one-team-or-one-person: backlog
  3-4-opening-a-day-to-see-who-is-actually-on-it: backlog
  3-5-an-admin-changes-a-team-s-shift-type-on-one-date: backlog
  3-6-an-admin-adds-removes-or-replaces-someone-on-a-shift: backlog
  epic-3-retrospective: optional

  epic-4: backlog
  4-1-a-member-sees-their-own-hours-split-by-band: backlog
  4-2-an-admin-sees-hours-for-everyone-and-can-explain-any-figure: backlog
  epic-4-retrospective: optional

  epic-5: backlog
  5-1-an-admin-records-leave-and-sees-what-it-costs-first: backlog
  5-2-an-admin-amends-or-deletes-leave-and-the-balance-follows: backlog
  5-3-a-collision-with-a-rostered-shift-becomes-a-visible-conflict: backlog
  5-4-an-admin-decides-each-conflict-on-its-own-screen: backlog
  5-5-a-configuration-change-cannot-quietly-erase-a-pending-decisi: backlog
  epic-5-retrospective: optional

  epic-6: backlog
  6-1-a-member-opens-the-app-and-already-has-their-answer: backlog
  6-2-a-24-hour-duty-reads-as-one-duty-not-two-unrelated-shifts: backlog
  6-3-an-admin-opens-the-app-and-sees-what-needs-them: backlog
  epic-6-retrospective: optional
