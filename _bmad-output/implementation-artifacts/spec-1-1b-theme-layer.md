---
title: 'Story 1.1b — The theme layer'
type: 'feature'
created: '2026-09-03'
status: 'done'
review_loop_iteration: 1
baseline_commit: '935a7e4c54277e71b5b17d04d10f4a85594517c1'
context:
  - '{project-root}/_bmad-output/implementation-artifacts/epic-1-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** `apps/web/src/index.css` is a bare `@import 'tailwindcss'` — no `@theme` block, no colour token, and none of the shadcn base variables its own `components.json` presumes with `cssVariables: true`. `iconLibrary: lucide` is declared with no `lucide-react` installed, and the face DESIGN.md relies on for Croatian diacritics is absent.

**Approach:** Make `components.json`'s contract true. Author the shadcn neutral base plus DESIGN.md's 46 brand tokens in OKLCH, each in light and dark, selected purely by `prefers-color-scheme`. Pin the icon and font packages the config already promises. Prove it with three tests: every token paired, every contrast ratio measured, the shipped face covering Croatian.

## Boundaries & Constraints

**Always:**
- Every token defined in **both** themes (UX-DR1). Light is bare `:root`; dark is a `prefers-color-scheme: dark` block only.
- Values converted from DESIGN.md's `colors:` front matter — never invented or re-derived.
- Exact pins (`.npmrc` sets `save-exact=true`). Tests are `.ts`, node environment, no jsdom (AD-15).

**Ask First:**
- Any theme toggle, setting or persisted preference — UX-DR2 forbids the surface, so no `.dark` hook or `data-theme` attribute either.
- A `shift-day`, `shift-night` or any semantically-named ramp token (UX-DR6); `destructive` for anything but an unresolved conflict (UX-DR4).
- A contrast failure in ramp slots 3–6 — `[ASSUMPTION]`, unexercised by the pilot, so a failure is a design question, not a value to nudge.

**Never:**
- No user-facing string of any kind — no screen, navigation or copy (1.1c, 1.1d).
- No i18next, formatting module or resource file (1.1c). No organization accent wiring (1.4).
- No shadcn primitive generated here; only the variables one would consume.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|---|---|---|---|
| Either theme | light, or `prefers-color-scheme: dark` | Every token resolves to that theme's value | An unpaired token fails the token test |
| Contrast | any foreground/background pair | ≥4.5:1 body, ≥3:1 large text and UI | Test names the pair and measured ratio |
| Diacritics | the shipped Geist face | Covers č ć ž š đ Č Ć Ž Đ Š | Test names missing codepoints |
| Conversion | DESIGN.md hex → OKLCH | 46 tokens, all `oklch(...)` | A stray hex value fails the token test |

</frozen-after-approval>

## Code Map

- `_bmad-output/planning-artifacts/ux-designs/ux-shift-2026-09-02/DESIGN.md` -- **read-only source of truth**: its `colors:` front matter is the authoritative 46-token hex enumeration. Shape: `primary`/`destructive` x4 each (base, foreground, dark, foreground-dark) = 8; `shift-slot-1`...`6` x4 = 24; `shift-nonworking` x4; `modifier-leave`/`modifier-uncovered` x4 each; `modifier-overridden` x2, no foreground pair = 46 exactly. The "fourth modifier signal" is conflict, reusing `destructive` (UX-DR4) rather than owning tokens. The two overlay modifiers are `rgba(...)`, not solid.
- The 46 OKLCH conversions were independently verified exact against DESIGN.md by two reviewers (worst round-trip drift 0.066/255) and are now the shipped values in `apps/web/src/index.css`. Reuse them; do not re-convert. `test/theme-fidelity.test.ts` re-proves the round-trip on every run.
- `apps/web/src/index.css` -- reverted to a bare `@import 'tailwindcss'`. Everything lands here. **Three layers are required, not two:** the shadcn neutral base, the brand delta, and — the gap that caused this loopback — a rule that actually *applies* them. Tailwind 4's preflight paints no page background, so shadcn's `@layer base { * { @apply border-border } body { @apply bg-background text-foreground } }` is load-bearing. Without it the built CSS defines every token and consumes none.
- **The shadcn base is 28 names**, fetched from `https://ui.shadcn.com/r/colors/neutral.json` (`cssVarsV4`): `background`, `foreground`, `card`(+`-foreground`), `popover`(+`-foreground`), `secondary`(+`-foreground`), `muted`(+`-foreground`), `accent`(+`-foreground`), `border`, `input`, `ring`, `chart-1..5`, `sidebar`(+6 more). `primary`/`destructive` come from the delta instead. Two stock values already breach this story's own thresholds: light `muted-foreground` on `muted` is 4.34:1, and light `ring` on `background` is 2.59:1.
- `apps/web/components.json` -- `cssVariables: true`, `baseColor: neutral`, `iconLibrary: lucide`, `tailwind.config: ""`. CSS-first Tailwind 4 via `@tailwindcss/vite`: no JS config exists and none should be created.
- `test/` -- its own vitest project. **Idiom:** `test/supabase-scaffold.test.ts` for parsing a shipped file (`node:fs`, root via `fileURLToPath(new URL('..', import.meta.url))`, never `process.cwd()`); `apps/web/src/router.test.ts` for phrasing — JSDoc citing the story and why the test exists, `describe` as a plain-English invariant, `it` as a behavioural clause, never "should". Guard vacuous passes before asserting a filtered set is empty.
- `apps/web/dist/assets/*.css` -- the built stylesheet. Source-only assertions cannot see whether a token is consumed, so the application check must read the build and use `it.skipIf` when `dist` is absent, per `test/static-hosting.test.ts:20`.

Pinned versions, directory shape and AD-14/15/17 are in the loaded `epic-1-context.md` — do not re-derive them.

## Tasks & Acceptance

**Execution:**
- [x] `apps/web/package.json`, root `package.json` -- DONE and kept through the loopback: `lucide-react@1.39.0`, `@fontsource-variable/geist@5.3.0`, `culori@4.0.2` and `@types/culori@4.0.1`. Do not re-derive; see the change log for why 1.39.0.
- [x] `apps/web/src/index.css` -- three layers: the 28-name shadcn neutral base in OKLCH, the 46 brand values converted from DESIGN.md, and an `@layer base` rule applying `bg-background text-foreground` to `body` plus the default border colour. Add `color-scheme: light dark` so UA-painted surfaces (scrollbars, form controls, the canvas behind the root) follow the theme. Import Geist, bind `--font-sans`. **Do not** author a `tabular` utility — Tailwind 4 already ships `tabular-nums`, which is the name DESIGN.md uses. UX-DR40's requirement is that figures which align use it; no surface aligns figures until the Epic 4 hours table, so the requirement is carried in deferred-work rather than satisfied here.
- [x] `test/theme-css.ts` -- one shared helper: comment-blind source, brace-matched light and dark scopes, and a `readToken(theme, name)`. Both colour tests import it, so they cannot drift apart or disagree about whether comments are stripped. No `expect()` at module scope — a missing dark block must fail as a named assertion, not a collection error.
- [x] `test/theme-tokens.test.ts` -- over **both** the 23 brand names and the 28 base names: present in each theme, every value `oklch(...)`, no hex, no `.dark`/`data-theme` hook, no semantically-named ramp token, and **every token's dark value differs from its light value** (a copy-pasted dark block currently passes everything). Assert the `@theme inline` block maps each token to a `--color-*` entry. Drop the "exactly 46" count — it is capped at 23 per theme by construction and can only restate the assertions above it.
- [x] `test/theme-applied.test.ts` -- read the built CSS: `var(--background)` and `var(--foreground)` are actually consumed, and `color-scheme` is declared as a property, not only as a media query. `it.skipIf` when `dist` is absent. This is the assertion whose absence let the first derivation ship a theme that never rendered.
- [x] `test/theme-contrast.test.ts` -- via culori, in both themes: brand solid pairs and base surface/foreground pairs at 4.5:1; `ring`/`sidebar-ring` against their surface at 3:1; the two alpha overlays composited over every fill except `shift-slot-2` at 3:1; **the composited hatch against its surrounding fill** at 3:1; and `modifier-overridden` against **each ramp fill it borders**, not against the page background. `muted-foreground` (stock 4.34:1), `ring` (stock 2.59:1) and `sidebar-ring` (stock 2.48:1) were all Ask First — resolved by lightness-shifting each; the suite now asserts they pass at their shifted values, not that they fail.
- [x] `test/typography-coverage.test.ts` -- parse the imported Geist CSS from `apps/web/node_modules` (isolated linker: it is not at the root). Assert a declared `unicode-range` covers all ten of **č ć ž š đ Č Ć Ž Đ Š**. UX-DR8's `◷` U+25F7 and `◌` U+25CC are measured and asserted **absent** — neither is in any Geist subset, and the fix belongs to Epic 3's `shift-cell`; see the change log and deferred-work. Resolve a subpath import correctly — `@import '.../wght.css'` must not become `wght.css.css`. No `readFileSync` at module scope, or the guard assertions never run.
- [x] `test/theme-fidelity.test.ts` -- parse DESIGN.md's `colors:` front matter and assert every shipped OKLCH value round-trips to its hex, excepting the two recorded `shift-nonworking-foreground` deviations. Contrast only catches lightness drift; a hue or chroma slip leaves every ratio intact.
- [x] `_bmad-output/implementation-artifacts/deferred-work.md` -- correct the two errors in the entries this story added: the DEPLOY.md hedge (verified: DEPLOY.md contains no `jsdom`; the only other copy is `spec-1-1a-deployable-shell.md:107`), and the overlay range, which is 4.37–5.28:1 in light and 4.91–6.60:1 in dark, with the slot-2 own-foreground figure 9.49:1 for leave and 8.30:1 for uncovered.
- [x] `_bmad-output/implementation-artifacts/sprint-status.yaml` -- the split comment still describes 1.1b as "theme + i18n + Croatian sign-in, NOT STARTED". Update it to the three-way split this session made.

### Review Findings

- [x] [Review][Patch] `--sidebar-ring` deviation from stock shadcn shipped with no changelog entry — the CSS header names it as a fourth "measured and approved" deviation (L 0.708 → 0.654, clearing 2.48:1 → 3:1 on `--sidebar`), but unlike the other three deviations it has no "Human-approved" callout anywhere in the Spec Change Log. Confirmed by Perolipotic (2026-09-03) as an approved deviation, same reasoning as `--ring`. **Fixed:** added a dedicated changelog entry mirroring the other three. [apps/web/src/index.css:16-27]
- [x] [Review][Patch] Spec Change Log's stated OKLCH numbers for `shift-nonworking-foreground`, `muted-foreground`, and `ring` don't match what shipped — the "Implementation" entry says nonworking-foreground moved to L 0.5411/0.6105, but the shipped/header value is 0.5361/0.6165; the "Re-derivation" entry says muted-foreground → 0.547 and ring → 0.669, but shipped is 0.542 and 0.665. The CSS header comment already has the correct numbers — only the changelog prose is stale. **Fixed:** corrected both changelog entries to the shipped values and hex. [_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md changelog entries; apps/web/src/index.css:68,73,106,164]
- [x] [Review][Patch] Tasks & Acceptance still says to "Expect `muted-foreground` (4.34:1) and `ring` (2.59:1) to fail" in `test/theme-contrast.test.ts`, but the Re-derivation entry resolved both via a lightness shift and the shipped test asserts `toBeGreaterThanOrEqual(AA_BODY)` uniformly — they now pass. The task line is stale and misleads a reader into thinking the accessibility question is still open. **Fixed:** rewrote the task line to describe the resolved state, including `sidebar-ring`. [_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md Tasks & Acceptance; test/theme-contrast.test.ts:111,125]
- [x] [Review][Patch] `test/theme-fidelity.test.ts`'s `APPROVED` deviation map only names `shift-nonworking-foreground`; the three shadcn-sourced deviations (`muted-foreground`, `ring`, `sidebar-ring`) have no equivalent "this is a deliberate deviation" test, and are only incidentally protected by `theme-contrast.test.ts` failing if reverted. **Fixed:** `theme-fidelity.test.ts`'s map is DESIGN.md-round-trip specific and structurally cannot cover shadcn-sourced tokens (they have no DESIGN.md entry), so instead added a named "stays shifted, not reverted to stock" describe block to `test/theme-contrast.test.ts` covering all three. [test/theme-contrast.test.ts — new describe block before "the inherited border contrast is pinned"]
- [x] [Review][Patch] `test/theme-applied.test.ts`'s "references the theme tokens specifically" allowlist regex omits `secondary` and `chart-`, unlike the full base-token set. Currently masked because `--background`/`--foreground`/`--border` are unconditionally emitted by `@layer base`, so the assertion always finds a match regardless. **Fixed:** added both prefixes to the regex. [test/theme-applied.test.ts:83-91]
- [x] [Review][Patch] `test/theme-contrast.test.ts`'s `--border`/`--input` composited-contrast check asserts a wide band (`> 1` and `< AA_LARGE`, i.e. 1–3:1) rather than the specific measured ratio (1.25–1.47:1). deferred-work.md and the story's own comment claim these values "cannot drift while the question is open," but a future accidental edit moving the ratio anywhere within 1–3 would pass silently. **Fixed:** pinned each theme/token pair to its measured ratio via `toBeCloseTo`, alongside the existing band checks. [test/theme-contrast.test.ts:175-176]
- [x] [Review][Defer] `theme-applied.test.ts`'s build-freshness check (`toBeGreaterThan`, strict) can spuriously fail a genuinely fresh build on filesystems/environments with coarse (e.g. 1-second) mtime resolution. Loosening to `>=` would reopen the exact stale-build blind spot the test exists to catch; a real fix needs a different mechanism (e.g. content hash), which is a larger design change than this review should force. [test/theme-applied.test.ts:56-62] — deferred, pre-existing tradeoff in the freshness-check design

**Acceptance Criteria:**
- Given a clean checkout, when `pnpm install && pnpm build && pnpm lint && pnpm typecheck && pnpm test` runs, then every command exits 0
- Given the **built** CSS, when inspected, then `var(--background)` is consumed by at least one rule and `color-scheme` is declared as a property
- Given the built CSS, when inspected, then light and dark both ship and the only selector distinguishing them is `prefers-color-scheme` (UX-DR2)
- Given the rendered shell, when OS appearance is switched, then it repaints with no reload and no flash of the wrong theme
- Given the bundle, when searched, then it holds no user-facing text beyond the existing `<title>`

## Spec Change Log

- **Implementation, 2026-09-03 — `lucide-react` pin lowered 1.40.0 → 1.39.0.** Triggered by `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION`: 1.40.0 was published 2026-09-03T08:52Z, inside pnpm 11's 24-hour supply-chain cutoff. The first implementation attempt worked around this by adding `minimumReleaseAgeExclude` to `pnpm-workspace.yaml` — exempting a hours-old package from the guard. Reverted; the pin moved to 1.39.0 (published 2026-09-01) instead, which clears the cutoff with the guard untouched. **Known-bad state avoided:** a supply-chain policy silently disabled in a shared config to accommodate a version choice that had no reason to be the newest. **KEEP:** never weaken `pnpm-workspace.yaml` or `.npmrc` policy to satisfy a pin; move the pin.

- **Implementation, 2026-09-03 — `shift-nonworking-foreground` lightness-shifted in both themes, a recorded deviation from DESIGN.md.** The contrast test measured DESIGN.md's values at 2.58:1 (light, `#8d9aa7` on `#f0f3f6`) and 3.76:1 (dark) against their own fill; UX-DR7 keeps that label always visible, so 4.5:1 applies. Light L 0.6801 → 0.5361 (`#636f7b`), dark L 0.5665 → 0.6165 (`#7a8794`) — the minimum shift clearing AA, hue and chroma untouched. Human-approved. **Known-bad state avoided:** a permanently unreadable label on every non-working cell, in a palette nobody had ever measured. **KEEP:** DESIGN.md stays the source of truth — deviations are recorded in both the change log and the stylesheet header, never made silently.

- **Implementation, 2026-09-03 — overlay modifiers held to 3:1, and `shift-slot-2` excluded from the overlay sweep.** UX-DR8 describes leave and uncovered as a hatch plus a glyph, not text, so WCAG's 3:1 graphical-object threshold governs them; that legitimises `modifier-uncovered` on slot-3 at 4.37:1 rather than nudging an `[ASSUMPTION]` token. Slot-2 is the one inverted slot — dark navy in the *light* theme — so fixed dark modifier foregrounds measure 1.84:1 and 1.78:1 over it. No token value fixes that; the resolution is a `shift-cell` composition rule deferred to Epic 3. Human-approved. **Known-bad state avoided:** asserting, in the token layer, a composition that no component implements — which would have forced either a permanently red suite or a fabricated token. **KEEP:** the overlay sweep still covers all six other fills in both themes; do not narrow it further.

- **Review loopback 1, 2026-09-03 — bad_spec: the theme was defined but never applied.** Three review layers ran; the blind hunter found that no rule paints `--background`/`--foreground` onto `html`/`body`, and the built CSS contained **zero** `var(--background)` consumers. Tailwind 4's preflight paints no page background, so shadcn's `@layer base` block is load-bearing and was absent — the shell rendered browser-default white in both themes and acceptance criterion 3 was not met. The root cause is this spec, not the implementer: the Tasks named the base and the delta but never said to apply them, and no acceptance criterion was checkable against the build. Amended: a third layer plus `color-scheme` in the CSS task, a `theme-applied.test.ts` reading the built stylesheet, and an AC phrased against the build rather than a manual eyeball. Related gaps the same under-specification produced, all now named in Tasks: the 28 base tokens and the 51 `@theme inline` mappings were asserted by nothing (both proven by mutation — deleting `--border`, `--ring` and `--color-shift-slot-4` left the suite green); the hatch stripe was never measured, only the glyph on it; `modifier-overridden` was measured against the page background rather than the ramp fills it borders (3.41:1 real versus 4.44:1 asserted); nothing asserted a dark value differs from its light one; and the "exactly 46" test was tautological. Code reverted to baseline (the superseded derivation was later squashed away rather than kept in history); the dependency installs were kept, since they are correct and re-deriving them only re-runs the supply-chain check. **Known-bad state avoided:** shipping a token layer, measured to the digit, that no pixel on screen ever consumes. **KEEP:** the 46 OKLCH conversions are verified exact — reuse, do not re-convert; culori for measurement; `unicode-range` parsing over font-binary parsing; comment-blind scanning; resolving the font package from `apps/web/node_modules`, not the root; and the three human decisions above, which stand unchanged.

- **Re-derivation, 2026-09-03 — three corrections found by running the amended spec.** (1) *The hatch assertion was unsatisfiable as written.* The amended Tasks called for the composited stripe to clear 3:1 against its fill; a 16-22% alpha tint over its own base has an arithmetic ceiling of 1.26:1, so the threshold demanded the impossible. WCAG's 3:1 governs the signal that *identifies* a state, which UX-DR8 assigns to the glyph; the hatch is redundant texture, and the question for a texture is whether the eye can see it. Re-expressed as CIEDE2000 >= 3 (minimum measured 6.58, against a ~2.3 just-noticeable bound). **KEEP:** measure a translucent tint perceptually, never as a contrast ratio. (2) *Six shadcn base tokens are identical across themes by design* — the five neutral chart greys and `sidebar-primary-foreground` — so the blanket differs-between-themes assertion was wrong for them; they are now asserted *equal*, which locks the knowledge and reports a future upstream change instead of quietly widening. (3) *Two stock shadcn light values breached this story's own thresholds*: `muted-foreground` on `muted` at 4.34:1 and `ring` on `background` at 2.59:1. Both adjusted by the minimum lightness shift (L 0.556 -> 0.542 `#6f6f6f` and 0.708 -> 0.665 `#949494`); both dark values already passed and are untouched. Human-approved. The `ring` case is the substantive one — a focus indicator at 2.59:1 is a keyboard-accessibility defect that WCAG 2.4.11 exists for. **Known-bad state avoided:** inheriting an upstream default uncritically because it came from a reputable source.

- **Code review, 2026-09-03 — third stock shadcn light value (`--sidebar-ring`) had the same defect and the same fix, but no changelog entry.** `--sidebar-ring` shares `--ring`'s stock value and measured the same class of failure — 2.48:1 on `--sidebar`, below the 3:1 WCAG 2.4.11 requires for a focus indicator — and was already lightness-shifted in the shipped CSS (L 0.708 -> 0.654 `#909090`) alongside `--ring`, but the Re-derivation entry above only named two of the three deviations it should have covered. Confirmed human-approved by Perolipotic during code review, same reasoning as `--ring`. **Known-bad state avoided:** an accessibility-affecting color decision shipping with no audit trail, silently contradicting this story's own KEEP rule that deviations are "recorded in both the change log and the stylesheet header, never made silently." **KEEP:** when a shadcn base token breaches a threshold, check its siblings for the same stock value before writing the changelog entry — `--ring` and `--sidebar-ring` share defaults and one was missed on the first pass.

- **Re-derivation, 2026-09-03 — UX-DR8's state glyphs deferred to Epic 3.** `◷` (U+25F7) and `◌` (U+25CC) are in no Geist subset and fall back to the OS face. Real, and under UX-DR37/Q21 they are the non-colour carriers of meaning — but UX-DR40 scopes its coverage requirement to Latin Extended-A, nothing renders a glyph before Epic 3's `shift-cell`, and the fix is a choice between `lucide-react` (already pinned, and what `components.json` declares) and Geist-covered substitutes. Human-approved. `test/typography-coverage.test.ts` asserts the *absence* of coverage, so a future Geist release adding them fails the test and reopens the question. **KEEP:** when a finding's fix belongs to a component that does not exist, assert the measurement rather than the requirement.

## Design Notes

**Why the shadcn base is in scope.** `cssVariables: true` with `baseColor: neutral` is the contract that primitives read `--background`, `--foreground`, `--border` and `--ring` from CSS. Story 1.1a wrote `components.json` but never ran an init that emits them, so shipping only the brand delta leaves the first primitive pointing at undefined properties — which renders as invisible text rather than raising an error. The delta sits on the base; it does not replace it.

**Why ramp tokens are numbered, never named.** Shift types take slots in creation order and the always-visible label carries the distinction (UX-DR6). A `shift-night` token would encode an organization's naming into the theme, and no code may branch on an admin-entered name. Past six, a slot repeats.

**The two overlay modifiers.** `modifier-leave` and `modifier-uncovered` are `rgba(...)` deliberately — they compose over a slot fill rather than replacing it (UX-DR8), so the conversion must preserve alpha.

**The font risk is the subset, not the glyph.** `@fontsource-variable/geist@5.3.0` ships five subsets. Every Croatian diacritic sits in `latin-ext`; the `latin` subset (U+0000–00FF plus scattered singles) contains none of them. The package's default entry declares all five, so importing `@fontsource-variable/geist` is correct — but importing a narrower entry silently drops Croatian and produces exactly the mid-word fallback DESIGN.md warns about. That is what the coverage test guards.

## Verification

**Commands:**
- `pnpm install && pnpm build && pnpm lint && pnpm typecheck && pnpm test` -- expected: exit 0
- `grep -c "oklch(" apps/web/src/index.css` -- expected: 102 (51 tokens x 2 themes). A `>= 46` floor would pass with half the theme deleted; it predates the 28-name base layer.
- `test -z "$(pnpm ls --recursive jsdom 2>/dev/null)" && echo NO-JSDOM` -- expected: `NO-JSDOM`. Note `pnpm ls` exits 0 whether or not it matches, so the `|| echo` form used in spec 1.1a can never report absence — test the output, not the exit code.
- Forbidden-construct **and hex** scanning is done comment-blind by `test/theme-tokens.test.ts`. No raw `grep` belongs here: the stylesheet header documents `.dark`, `data-theme` and the DESIGN.md hex values of every approved deviation — the latter required by this log's own KEEP rule — so a plain scan fails on its own documentation.

**Manual checks:**
- Switching OS appearance repaints the shell with no reload and no flash of the wrong theme
