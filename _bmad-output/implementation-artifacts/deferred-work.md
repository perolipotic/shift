- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Build the 46-token theme layer, the i18next+ICU localization and formatting layer, and the static Croatian sign-in shell (story 1.1's "speaks Croatian in both themes" half).
  evidence: Story 1.1's full spec measured 3005 tokens against a 1600 ceiling because the story carries four one-time foundations plus a screen. Split at the seam between deployable infrastructure and the themed/translated presentation layer; this half depends on the merged 1-1a workspace but is separately reviewable and testable. Covers UX-DR1, UX-DR2, UX-DR3, UX-DR40 and L1-L7.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: RESOLVED — `supabase db reset` has now been run and passes. No action required.
  evidence: Docker Desktop's outbound networking recovered later in the same session. Verified against the real local stack (CLI 2.116.0, postgres 17.6.1.165): `supabase db reset` exits 0, applies `0001_extensions.sql` and loads `seed.sql`; `select extname, extversion from pg_extension` returns `btree_gist|1.7`; a probe table with `exclude using gist (member_id with =, during with &&)` was created and a second overlapping row refused with `conflicting key value violates exclusion constraint`, so AD-3's leave-overlap constraint is buildable on this extension exactly as planned; and `select count(*) from pg_tables where schemaname='public'` returns 0, confirming the seed creates no schema of its own. The `admin-auth` function was additionally served on the local edge runtime (`supabase-edge-runtime-1.74.3`) and answered `501 NOT_IMPLEMENTED`, `405 METHOD_NOT_ALLOWED`, `400 OPERATION_UNKNOWN` and `400 BODY_NOT_JSON` with the expected bodies.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Revisit typed linting (typescript-eslint) once TypeScript 7.1 ships a stable programmatic API.
  evidence: `typescript@7.0.2`'s only main export is `./lib/version.cjs` — the compiler is the native Go binary and the JS API is available solely under `./unstable/*`. `typescript-eslint@8.69.0` (latest) declares `typescript: ">=4.8.4 <6.1.0"` and needs `ts.createSourceFile`, so neither typed nor syntax-only TS parsing works through it. ESLint 10 has no built-in TS language. `eslint.config.js` therefore parses TS/TSX with `@babel/eslint-parser` + `@babel/preset-typescript` (syntax only, no type information); `tsc` owns type errors. The TypeScript 5.x fallback was explicitly declined by spec 1.1a.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Move `apps/web/src/routes/` to TanStack Router file-based routing once `@tanstack/router-plugin` catches up to the pinned router version.
  evidence: The architecture spine describes `routes/` as "TanStack Router file routes", but `@tanstack/router-plugin` tops out at 1.168.35 while `@tanstack/react-router` is pinned at 1.170.32 — the plugin is not published at the pinned version, and mixing majors of the generator and runtime risks a generated route tree that disagrees with the runtime. 1.1a therefore uses code-based routing (`src/router.ts` assembles `rootRoute.addChildren([...])` from the files in `routes/`), which keeps every route in `routes/` and makes the later switch mechanical.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: The function's `SHIFT_ALLOWED_ORIGINS` allowlist is not the effective CORS policy — the Supabase API gateway overrides it. Decide whether the allowlist stays as defence-in-depth or the gateway config becomes the real control point.
  evidence: Discovered while exercising the function on the local edge runtime, and it changes what the allowlist means rather than whether it works. Kong (the Supabase API gateway, 2.8.1) sits in front of every Edge Function invocation and (a) replaces our `Access-Control-Allow-Origin` with `*`, (b) injects `Access-Control-Allow-Origin: *` even on the replies where our handler deliberately emits no allow header at all, and (c) answers `OPTIONS` itself with `200` and `Access-Control-Allow-Methods: GET,HEAD,PUT,PATCH,POST,DELETE,OPTIONS,TRACE,CONNECT`, so our 204 preflight branch never runs through the gateway. Calling the edge runtime directly (`http://edge_runtime:8081/admin-auth`, bypassing Kong) reaches our handler and returns 501 as designed, which is what confirms the difference is the gateway and not the code. So a browser talking to the deployed function is governed by the gateway's permissive policy; ours is a second, stricter layer that only takes effect if the gateway ever stops overriding it. The handler's own behaviour is fully asserted in `test/admin-auth-boundary.test.ts`. Verify whether hosted Supabase's gateway behaves the same way as the local one before deciding; this sits with the already-deferred CORS/`verify_jwt`-preflight item.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Decide where the caller's JWT is verified, given `verify_jwt = true` in `config.toml` rejects a request before `admin-auth`'s handler runs.
  evidence: A browser CORS preflight carries no `Authorization` header, so with platform-level JWT verification enabled the `OPTIONS` branch and the handler's own `AUTHORIZATION_MISSING` reply can never execute in a deployed environment — the platform answers first, with a body carrying no stable `code`. Story 1.2 must verify the caller against the database anyway (AD-16), which is an argument for `verify_jwt = false` plus in-handler verification. Live local testing confirmed the gateway intercepts `OPTIONS`; confirm hosted behaviour before choosing. Related to the gateway/allowlist entry above.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Add a CI workflow running `pnpm install && pnpm build && pnpm lint && pnpm typecheck && pnpm test` on push and pull request.
  evidence: No `.github/workflows` or equivalent exists, so every invariant this story added — domain purity, key hygiene, the static-host fallback, the scaffold shape, the privileged boundary's transport — is enforced only when a human types the gate by hand. Two build-output tests additionally skip when `dist` is absent, so the ordering that makes them meaningful (build before test) is currently enforced by prose in DEPLOY.md rather than by a runner.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Add `apps/web/public/_headers` with a Content-Security-Policy and the standard security headers.
  evidence: The application's central invariant is key containment (AD-17), and a CSP restricting `connect-src` to the environment's Supabase origin is the cheapest available reinforcement of it. Nothing currently sets CSP, `X-Content-Type-Options`, `Referrer-Policy`, `frame-ancestors` or HSTS. Cloudflare Pages reads `_headers` from the same directory as the existing `_redirects`, so the mechanism is already in place.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Decide how Cloudflare Pages preview deployments reach `admin-auth`, since their per-commit hostnames are never on the origin allowlist.
  evidence: `SHIFT_ALLOWED_ORIGINS` is set to the staging hostname, but Pages assigns each preview deployment a `<hash>.<project>.pages.dev` host. If Preview is the designated staging front end, the documented staging verification cannot exercise the function from a preview build. Options: use the stable branch-alias hostname, accept a documented suffix pattern, or call the function same-origin.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Decide whether an empty `SHIFT_ALLOWED_ORIGINS` should be a configuration error rather than a silently accepted empty allowlist.
  evidence: `readConfiguration` fails fast on five key and URL problems but accepts a missing allowlist, yielding `allowedOrigins: []`. The result is a function that starts healthy and answers `curl` correctly while being unreachable from every browser — a failure that presents as an opaque network error with no server-side trace. An empty allowlist may be legitimate for server-to-server use, so this is a decision rather than a defect.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Add request hardening to `admin-auth` — a `content-type` check, a body-size bound, and abuse control — before story 1.2 makes the endpoint act.
  evidence: `await request.json()` runs with no content-type check and no size bound, and there is no rate limiting anywhere. Exposure is minimal today because every path terminates in 501 before touching data, but the guard should exist before the boundary gains the ability to create, update and ban users.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Give the SPA a not-found route and a boot-failure fallback once the localization layer exists.
  evidence: `_redirects` maps every path to `index.html` at 200 and the router registers no `notFoundComponent` or `defaultErrorComponent`, so a genuine typo renders a blank shell with HTTP 200 — and DEPLOY.md's `curl /some/deep/route` check passes for typos too. `main.tsx` throws `ROOT_ELEMENT_MISSING` into a white screen. Both need user-facing text, which story 1.1a forbids and story 1.1b introduces, so they belong with the i18n layer.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Make `index.html`'s `lang` attribute follow the active locale, and add the missing document metadata.
  evidence: `lang="hr"` is a build-time literal with no mechanism to change it, which contradicts the localization contract that adding a language requires no component or logic change — and `lang` is exactly what screen readers and browser translation depend on. Also absent: `meta name="description"`, `theme-color`, a favicon (currently a 200-serving blank shell), a web manifest, and a `noscript` message for an application that does not function without JavaScript.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Add `lucide-react` as a pinned dependency before the first shadcn component carrying an icon is generated.
  evidence: `apps/web/components.json` declares `"iconLibrary": "lucide"`, so the first `shadcn add` of any icon-bearing primitive (Button with an icon, Select, Command, Dialog close) emits an import that cannot resolve. Nothing breaks until then, which is why it is easy to hit unexpectedly during story 1.1b.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Decide whether production builds should publish source maps.
  evidence: `apps/web/vite.config.ts` sets `build.sourcemap: true` unconditionally, so `.map` files ship to the public deployment and expose full original sources. The code is shipped to the browser regardless, so this is disclosure of readability and internal structure rather than of secrets — but for an application whose stated central invariant is key containment it deserves an explicit decision. `sourcemap: 'hidden'` or gating on mode are the options.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Add `eslint-plugin-react-hooks`, `eslint-plugin-jsx-a11y` and a formatter to the lint setup.
  evidence: `eslint.config.js` documents why *typed* linting is absent (the TypeScript 7 API gap) but the React and accessibility rule sets do not depend on type information and are simply missing — so the rules most likely to catch real defects in `apps/web` are not running. `jsx-a11y` matters directly to the project's WCAG 2.1 AA target. There is also no Prettier config or `.editorconfig`, yet the diff is uniformly formatted, meaning a convention is being maintained by hand.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Constrain what `admin-auth` writes to its logs — codes, never operands or key material.
  evidence: The function logs `console.error('admin-auth could not construct its clients', cause)`, and the leak test asserts only on response bodies and headers. Nothing constrains what reaches Edge Function logs, where a client-construction error could echo connection material. Given how much else in this boundary is test-enforced, a log-hygiene rule plus an assertion is proportionate.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1a-deployable-shell.md`
  summary: Add the `apps/web` → `@shift/domain` workspace dependency edge when the first domain import lands.
  evidence: `apps/web/package.json` does not depend on `@shift/domain`, so `pnpm -r build` has no topological ordering between them and `@shift/domain` resolves through a gitignored, build-produced `./dist/index.js`. The Pages build command was changed to `--filter @shift/web...` so it will build dependencies once the edge exists, but the edge itself should be declared with the first import rather than discovered by a failing deploy.

- source_spec: none
  summary: Story 1.1c — the static Croatian sign-in shell, the not-found and boot-failure routes, and the locale-driven `lang` attribute plus document metadata (description, theme-color, favicon, web manifest, noscript).
  evidence: Split from story 1.1b at step-01's multi-goal check. 1.1b's theme layer and localization layer are each independently shippable — either merges and tests on its own without the other, and neither needs a screen to exist — while the sign-in shell is the sole consumer that depends on both. Splitting costs ordering rather than rework, since the screen waits on both foundations either way. Story 1.1 already measured 3005 tokens against a 1600 ceiling and was split once at 1.1a/1.1b for exactly this reason; carrying three foundations plus a screen plus four deferred items in one spec risked repeating it. The seam also preserves 1.1a's "no user-facing string of any kind" invariant through 1.1b, so every user-facing literal in the application enters in one reviewable story. Absorbs the three previously deferred entries that were parked for the i18n layer: the not-found/boot-failure routes, and the `lang` attribute plus missing document metadata.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Story 1.1c — the localization layer: i18next 26.4.1 with the ICU backend, the single locale-aware formatting module (date, time, time-range, number, month and day names, Croatian's three plural forms) taking a required IANA timezone argument, the `hr` resource file, the visible missing-key degradation, and an ESLint `no-restricted-syntax` guard making the "no user-facing literal" rule (L2) merge-blocking mechanically rather than by review.
  evidence: Split from 1.1b at step-02's token check. The combined theme-plus-localization spec measured 2906 tokens against a 1600 ceiling — an 82% overrun, close to the 3005 that forced the original 1.1a/1.1b split, and too large to close by trimming prose since it carried two full layers each with its own dependency set, config surface and three tests. The two layers share no file: the theme layer touches `apps/web/src/index.css` and `apps/web/package.json`, the localization layer touches `apps/web/src/i18n/`, `apps/web/src/main.tsx` and `eslint.config.js`. Neither blocks the other and they may be built in either order. Pins verified compatible with the stack on 2026-09-03: `i18next@26.4.1` (matches the spine pin exactly), `react-i18next@17.0.13` (peers react >=16.8, i18next >=26.2, typescript ^7), `i18next-icu@2.4.4`, `intl-messageformat@11.2.14` — note `i18next-icu` declares no runtime dependencies, so `intl-messageformat` is a peer that must be pinned explicitly. Binding rules are L1–L8 in `_bmad-output/specs/spec-shift/localization.md` (eight rules, not the seven this ledger previously recorded; L8 requires the organization's timezone, never the device's) plus UX-DR34–36.
  note: This renumbers the sign-in-shell entry appended earlier in this session from story 1.1c to story 1.1d. That entry is otherwise unchanged.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Epic 3's `shift-cell` must draw a leave or uncovered hatch and glyph in the underlying ramp slot's own foreground, not in the fixed `modifier-*-foreground` token.
  evidence: `shift-slot-2` is the one inverted slot — in the LIGHT theme its fill is `#1b2735`, a dark navy (the pilot's `Noć`). The modifier foregrounds are dark colours built for light fills, so composited over slot-2 they measure 1.84:1 (`modifier-leave`) and 1.78:1 (`modifier-uncovered`), failing both the 4.5:1 text and 3:1 graphical thresholds. Every other fill passes: the other six measure 4.37–5.28:1 in light and 4.91–6.60:1 in dark. No token value can fix this, because the same modifier foreground must work over both light and inverted fills; using the slot's own foreground (`#cbd8e6` for slot-2) instead measures 9.49:1 for `modifier-leave` and 8.30:1 for `modifier-uncovered`. Story 1.1b therefore excludes slot-2 from the overlay sweep in `test/theme-contrast.test.ts` rather than asserting a composition rule no component implements yet. A leave overlay on a night shift is a real state — it is exactly the leave-versus-rostered-shift collision Epic 5 turns into a conflict — so this must be resolved when `shift-cell` is built, and the assertion restored then.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Spec 1.1a's verification command `pnpm ls --recursive jsdom || echo NO-JSDOM` can never report absence and gives false assurance; the same flawed form may exist in DEPLOY.md.
  evidence: `pnpm ls` exits 0 whether or not it matches anything, printing an empty result when the package is absent. The `|| echo` form therefore never fires, so the check prints nothing and passes identically whether jsdom is absent (the invariant AD-15 requires) or present (the violation it exists to catch). Verified live: jsdom is genuinely absent from this tree — nothing on disk, and the only two `pnpm-lock.yaml` mentions are vitest's optional peer declarations at lines 1482 and 1505 — but the command proves none of that. Story 1.1b's own verification block uses `test -z "$(pnpm ls --recursive jsdom 2>/dev/null)" && echo NO-JSDOM` instead. Verified 2026-09-03: DEPLOY.md contains no `jsdom` reference at all, so the only other copy is `spec-1-1a-deployable-shell.md:107`. ACTION: tighten the AD-15 gate into something that can actually fail — a root test asserting jsdom is absent from `pnpm-lock.yaml`'s importers, which needs no build and no `pnpm ls`. Closing that closes this entry; spec 1.1a's own line is closed history and stays as-is. Note `pnpm ls` also defaults to depth 0, so a transitively-installed jsdom would still escape both forms — use `--depth Infinity` if this is ever tightened into a real gate.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Epic 3's `shift-cell` must not render UX-DR8's state marks as the Unicode characters `◷` (U+25F7) and `◌` (U+25CC) — neither is covered by Geist, so both fall back to whatever face the operating system supplies.
  evidence: Measured 2026-09-03 against `@fontsource-variable/geist@5.3.0`'s five declared `unicode-range` sets: the ten Croatian diacritics are all covered by latin-ext, but U+25F7 and U+25CC fall in no range at all. Under UX-DR37 and Q21 these glyphs are the non-colour carriers of meaning for leave and uncovered — the reason the hatch is not colour-alone — so inconsistent cross-platform rendering is a correctness problem rather than a cosmetic one. Two candidate resolutions: use `lucide-react` (already pinned at 1.39.0, and the icon library `components.json` declares), which removes font coverage from the question entirely since SVG renders identically everywhere; or substitute Unicode marks that Geist does cover. The first also gives the marks a consistent optical weight with every other icon in the interface. `test/typography-coverage.test.ts` asserts the *absence* of coverage, so if a future Geist release adds these codepoints the test fails and this entry can close. UX-DR40 itself scopes its coverage requirement to Latin Extended-A and does not mention these marks, which is why 1.1b does not gate on them.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Ramp slots are not mutually distinguishable by luminance — `shift-slot-1` against `shift-slot-6` measures 1.00:1 in light and `shift-slot-4` against `shift-slot-6` 1.02:1 in dark — so two adjacent Shift Types are separated by hue alone.
  evidence: Shift Types take ramp slots in creation order (UX-DR6), so an organization with several types will place visually adjacent fills next to each other in the calendar. Measured perceptually with the CIEDE2000 criterion this same story adopted for the hatch, however, adjacent slots differ by ΔE 9.72 (light) and 8.19 (dark) — roughly three times the perceptibility bound of 3 set in `test/theme-contrast.test.ts`. So the slots are distinguishable to normal vision and the luminance figure alone overstates the problem; what remains open is colour-vision deficiency, since hue-only separation is exactly what fails for the ~8% of men with red-green CVD, and UX-DR37/Q21 forbid meaning by colour alone. DESIGN.md's answer is that the always-visible label carries the distinction (UX-DR7), which may well be sufficient — but slots 3-6 are marked `[ASSUMPTION]` and unexercised by the pilot, so this has never been tested with a real multi-type roster. Needs either an explicit recorded decision that the label is the sole distinguisher, or a palette revision giving the slots luminance separation. Belongs with Epic 3's calendar surfaces, where adjacent slots first appear together on screen.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: All 51 `@theme inline` token mappings are tree-shaken out of the production build, so no `bg-shift-slot-3` or `text-modifier-leave-foreground` utility actually exists yet — and only a source-level assertion covers them.
  evidence: `grep -c -- "--color-shift" apps/web/dist/assets/*.css` returns 0. Tailwind 4 emits a utility only where it is used, and nothing uses these until Epic 3, so this is expected rather than broken. But `test/theme-tokens.test.ts` asserts the mappings exist by reading `index.css`, which is precisely the class of check review loopback 1 concluded was insufficient — the same reasoning that produced `test/theme-applied.test.ts`. The first component consuming a ramp token should add a build-level assertion that the utility resolves, since a mapping typo would otherwise surface as an unstyled cell rather than a failure.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Decide whether `--border` and `--input` need 3:1 against their surface once the 1.1d sign-in form exists; they currently composite to 1.25–1.47:1.
  evidence: Stock shadcn values, unchanged by this story and now pinned by `test/theme-contrast.test.ts` so they cannot drift while the question is open. WCAG 1.4.11 requires 3:1 for a control boundary only where that boundary is the component's sole visual affordance — which is exactly a shadcn text input, whose fill matches the page. Story 1.1d introduces the first form. Raising them to 3:1 (L 0.664, `#939393` in light) would visibly heavy every border in the interface, so this is a design decision rather than a defect to patch. Dark composites to 1.25–1.47:1 too, because `--border` is `oklch(1 0 0 / 10%)` — note a raw contrast reading there reports a meaningless 19.79:1 unless the alpha is composited first.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Dark `--sidebar-primary: oklch(0.488 0.243 264.376)` is a saturated stock shadcn blue — the only chromatic value in an otherwise neutral base — and has never been reconciled with the brand `--primary`.
  evidence: Every other shadcn neutral base token is achromatic (chroma 0); this one is not, and it ships a second blue alongside DESIGN.md's `#1F6FB2`. Light `--sidebar-primary` is the neutral `oklch(0.205 0 0)`, so the two themes disagree about whether the sidebar's primary is chromatic at all. DESIGN.md's `colors:` front matter does not mention sidebar tokens, so there is no design intent recorded either way. Either drop it to the neutral the rest of the base uses, or map it to the brand `--primary`, or record why a second blue is deliberate. Nothing renders a sidebar until the Epic 1 navigation shell.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: Nothing verifies Geist Variable actually carries the `tnum` OpenType feature, so the `tabular-nums` story 1.5a now applies may be a silent no-op. (The "not applied anywhere" half closed with 1.5a.)
  evidence: Story 1.1b binds Geist and deliberately does not author a `tabular` utility, since Tailwind 4 already ships `tabular-nums` — but shipping a utility is not the same as applying it. No surface aligned figures until story 1.5a, whose leave-allowance column is the FIRST — it now applies `tabular-nums` with `text-right`, pinned per column, so the first half of this entry is closed. **The second half is still open and is now load-bearing rather than hypothetical**: nothing asserts that Geist Variable actually carries the `tnum` OpenType feature, so if it does not, the utility 1.5a applies is a silent no-op and the column wobbles exactly as DESIGN.md warns, with a green suite. Verifying a font feature means reading the binary's GSUB table or rendering, neither of which the node suite does today — which is why 1.5a applied the utility and left this open. Epic 3's calendar columns and Epic 4's hours table inherit the same exposure.

## Deferred from: code review of spec-1-1b-theme-layer (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1b-theme-layer.md`
  summary: `test/theme-applied.test.ts`'s build-freshness check (`expect(built).toBeGreaterThan(source)`, strict) can spuriously fail a genuinely fresh build on filesystems/environments with coarse (e.g. 1-second) mtime resolution.
  evidence: Deferred rather than patched because the obvious fix — loosening to `toBeGreaterThanOrEqual` — reopens the exact stale-build blind spot this test exists to catch (deleting the whole `@layer base` block and running vitest without rebuilding left all 443 tests green before this check was added). A real fix needs a different mechanism, e.g. a content hash or a build marker file, which is a larger design change than a code-review patch should force. [test/theme-applied.test.ts:56-62]

## Deferred from: spec-1-1c-localization-layer (2026-09-03)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `formatTimeRange` and `formatDayMonthRange` take two `Date` instants, but a Shift Type's times are stored as SQL `time` without zone — so the first `shift-cell` has no instant to pass and no sanctioned way to render `19:00–07:00` from two clock values.
  evidence: The frozen "Always" rule requires every date/time entry point to take a REQUIRED `timeZone`, and a bare clock string has no zone to require, so a `formatClockRange('19:00', '07:00')` entry point cannot be added without renegotiating that rule. Meanwhile the data-model convention is explicit — "times as `time` without zone, clock arithmetic in integer minutes, no `timestamptz` outside `created_at`" — so a shift type genuinely has no date attached, and Epic 3's `shift-cell` renders exactly the range whose literal UX-DR34 binds. Three candidate resolutions: (a) the surface synthesizes instants from the roster date plus the shift's times, which is correct but puts date arithmetic in the surface where L6 says formatting belongs to one layer; (b) the module gains a zone-free clock entry point taking `{ hours, minutes }` or an integer-minutes pair, with a recorded exception to the required-zone rule justified by there being no instant to misinterpret; (c) the module gains an instant-building helper that takes the date, the clock time and the zone. Must be decided before the first cell renders a time range, or the ad-hoc `${start}–${end}` template it forbids is what will get written. [apps/web/src/i18n/format.ts:131 and :144]

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `formatNumber`'s two-fraction-digit default is set by one matrix row and has never been checked against a real column of figures; the hours table is where it is actually decided.
  evidence: The binding I/O matrix requires `1234.5` → `1.234,50`, while CLDR `hr`'s own default renders it `1.234,5`, so the module pins `minimumFractionDigits === maximumFractionDigits === 2` and takes an override argument. That is right for a money-shaped column and wrong for several plausible ones: leave allowance in days reads better as `12,5` than `12,50`, and an integer count would render `1.234,00` unless the caller remembers to pass `0`. Nothing in the tree consumes it yet. Epic 4's hours table and the leave balances DESIGN.md names are the first real columns; when one lands, decide whether the default stays 2, becomes a per-quantity formatter (`formatHours`, `formatDays`), or moves to the caller — and note that the same decision governs UX-DR40's `tabular-nums`, since ragged decimal counts are exactly what makes a column wobble. [apps/web/src/i18n/format.ts:156-182]

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: The L2 ESLint guard catches `JSXText` and four attributes; a literal inside a JSX expression container, a string passed as a component prop, and a string handed to an imperative call all pass it.
  evidence: `no-restricted-syntax` over `apps/web/**/*.tsx` fires on non-whitespace `JSXText` and on string-valued `aria-label`, `placeholder`, `title` and `alt` — verified firing and staying silent across eleven cases in `test/localization-guard.test.ts`. It does not fire on `{'Danas'}`, on `<Badge label="Danas" />`, on `toast('Spremljeno')`, or on a template literal, and it cannot be widened cheaply: `label`, `name` and `value` are user-facing on some components and structural on others, and only type information distinguishes them — which this config deliberately does not have (TypeScript 7 ships no programmatic API until 7.1, so linting here is syntactic). Narrowness is a deliberate trade: a false positive on `className` would train people to disable the rule, which is worse than the gap. But the gap is real and L2 is merge-blocking, so story 1.1d — the first story authoring real screens with real components — should re-measure it against the shapes those screens actually produce, and consider an allowlist-shaped rule over component props once shadcn wrappers exist to name.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `I18nextProvider` is wired into `main.tsx` and nothing asserts it reaches a component, because AD-15 bans jsdom and no surface consumes `useTranslation` yet.
  evidence: Every localization assertion in this story runs against the module directly (`t` imported from `@/i18n`), which is why 63 tests pass while the React binding is covered by nothing at all — a provider passing the wrong instance, or `react-i18next` failing to find the context, would ship green exactly as the theme layer's unpainted-token defect did before `test/theme-applied.test.ts`. AD-15 forbids the obvious fix (a rendered component test), so the check has to be indirect: the first surface consuming `useTranslation` should assert at build level that the resolved string reaches the built bundle, or the provider wiring should be replaced by module-level `t` usage if `useTranslation`'s re-render-on-language-change is never needed — which, with one locale and no language switch, it is not. Decide with story 1.1d, which authors that first surface.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `main.tsx` uses top-level `await initLocalization()`, so a rejected init renders nothing at all — a blank page with a console error and no fallback.
  evidence: Unreachable today: resources are bundled into the chunk, `init` performs no I/O, and the only throw inside it would be a malformed resource caught at build. But the shape is the one AD-14 warns about — the host answers every path with `index.html` at 200, so the client is what decides anything is wrong — and story 1.1c is explicitly forbidden from adding a boot-failure fallback ("No `lang` wiring, document metadata, not-found route or boot-failure fallback (1.1d)"). Story 1.1d owns it. When it lands, note that the fallback cannot itself be a translated string, since the failure being handled is the translation layer not starting. [apps/web/src/main.tsx:22]

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: Nothing checks a resource file for plural-category completeness; the typed `t` types keys only, so a locale added under L3 can ship a message missing a category and degrade to whichever form ICU falls back to.
  evidence: `hr` needs `one`/`few`/`other` and both messages carry all three, asserted at 1, 2, 5, 11, 21, 22, 101 and 1234. But the `CustomTypeOptions` augmentation infers key paths from `typeof hr` and says nothing about a message's internal shape, so `{count, plural, one {# dan}}` — with `few` and `other` missing — typechecks, lints clean, and renders the `one` form for every count. That is exactly the L3 "a resource file and no logic change" path: a second locale with different categories (`pl` adds `many`, `ar` has six) is where this first bites, and L5 promises an incomplete locale degrades safely rather than silently. A resource-file test that parses each ICU `plural` argument and asserts it declares every category `Intl.PluralRules(locale).resolvedOptions().pluralCategories` reports would close it; it was not written here because with one locale and two messages it would assert what the seven-count table already proves.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: The foreign-timezone proof in `format.test.ts` runs `format.ts` in a child process through Node's native type stripping, so it silently depends on that module never gaining an import.
  evidence: The in-process alternative does not work: `format.ts` memoizes its formatters, so a formatter built from the ambient zone before a mid-suite `process.env.TZ` mutation is immune to that mutation and an omitted `timeZone` would pass. A fresh process under `TZ=Pacific/Kiritimati` has no such hole, and it is a real guard — deleting `timeZone` from the one construction fails five assertions, four of them from the child. Measured 2026-09-04, after review: deleting `timeZone` from the one construction fails **6** assertions — 3 in-process and 3 from the child; an earlier note in this ledger said "5 assertions, 4 of them from the child", which was wrong on both counts (the child has four `it`s but one is the vacuous-pass guard confirming it ran under Kiritimati, which correctly does not fail on this mutation). But it loads the module by file URL with `--experimental-strip-types`, which resolves no `@` alias and runs no bundler, so the day `format.ts` imports anything non-relative the test breaks with a module-resolution error rather than a localization failure. The module header now says so in capitals, which is mitigation and not a fix. Either keep the module import-free (it has no reason not to be — it wraps `Intl` and nothing else), or move the proof to a vitest workspace project whose `TZ` is set in its own config. [apps/web/src/i18n/format.test.ts:266-319]

## Deferred from: code review of spec-1-1c-localization-layer (2026-09-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `formatNumber`'s two-fraction-digit default and ICU's `#` produce different numbers for the same value, inside the layer L6 says is the single source of every number.
  evidence: Measured against the pinned `intl-messageformat@11.2.14`: `formatNumber(1234.5)` returns `1.234,50` while `{count, plural, ...}` with `#` returns `1.234,5 dana`. Only reachable with a non-integer count, which `count.days` and `count.conflicts` never carry today, so it is latent rather than live. Resolve when the Epic 4 hours table decides the real decimal convention: either `formatNumber` loses its default or the ICU messages take `{count, number, ...}` styling. Related to the existing entry questioning the two-digit default.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: Localization rule L3 is unproven: no locale parameter exists anywhere, so adding a language edits the layer rather than only adding a resource file.
  evidence: `format.ts` hardcodes `LOCALE` and every entry point takes no locale; `index.ts` hardcodes `lng`, `supportedLngs` and `resources` to that one tag. L3 is binding and is the rule the whole centralized-layer architecture is justified by, yet nothing exercises it. The spec parked the throwaway-stub proof under Ask First and it was never requested, so this is a sanctioned omission rather than a defect — but it is the one binding rule with zero coverage. Cheapest close: add a stub second locale in a test, assert every format entry point and the resource lookup still resolve, and delete it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: No locale-aware collation path exists, while the L6 source scan forbids constructing `Intl` anywhere outside the formatting module.
  evidence: Sorting Croatian names correctly requires `Intl.Collator('hr')` for č/ć/š/ž ordering. `format.ts` exports no comparator, and `format.test.ts`'s offender scan now reports any `Intl` construction outside it. So the first name-sorted surface — story 1.5's member list at several hundred members with sort — has no sanctioned route and will either sort wrongly with `<` or trip a merge-blocking test. Add a comparator to the module when 1.5 lands.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: Two time shapes the mockups render most often have no entry point, and one of them contradicts `RANGE_DASH`'s documented role.
  evidence: `mockups/mobile-calendar-1.html:112` renders shift cells hour-only as `07–19`, and `:177` renders a duty as `10.09 07:00 → 11.09 07:00` with U+2192, not the en dash that `format.ts` documents as "the one range separator". `SHAPES` is a closed set containing neither. Mockups lose to the spine on conflict (`EXPERIENCE.md:11`), so these are undecided rather than binding — but story 1.1d and Epic 3's `shift-cell` will meet them, and the existing clock-string entry names neither literal.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `react-i18next`'s `useSuspense: true` default is left on with no `<Suspense>` boundary and no error boundary above it.
  evidence: Safe today only because `main.tsx` awaits `initLocalization()`, so `ready` is always true by first render — but nothing records that the safety depends on it, and `react: { useSuspense: false }` is not set. The first lazily-loaded namespace, or any `t` call before init, suspends into a tree with no fallback and renders blank. Arrives by a different route than the existing top-level-await entry, which is about a rejected init.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: UX-DR35 and L7 name six count categories; only two ship, both masculine, so no feminine or neuter declension example exists.
  evidence: The rules enumerate days, hours, shifts, conflicts, members and teams. `hr.json` carries `count.days` (`dan`) and `count.conflicts` (`konflikt`), both masculine. Croatian feminine declines differently (`1 smjena / 2 smjene / 5 smjena`) and Smjena = Team is the term a 1.1d or Epic 3 author reaches for most. The plural proof therefore covers one declension, and the next author has no reference for the pattern they need.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: The comment-stripping constants and the recursive directory walker are now duplicated across four test files with no shared home an `apps/web` test can import.
  evidence: `theme-css.ts`, `test/key-hygiene.test.ts`, `packages/domain/test/purity.test.ts` and `apps/web/src/i18n/format.test.ts` each carry their own copy. The root `test/` helpers are not importable from the `apps/web` vitest project, which is why the fourth copy exists. Either accept the duplication explicitly or give the helpers a shared package; letting it accrete silently is what makes the fifth copy inevitable.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `t()` with a null or undefined key returns an empty string, the blank outcome L5 forbids, because the missing-key handler never fires.
  evidence: Verified against the pinned packages: `t(null)` returns `""` while `t('')` correctly returns `⟦⟧`. Unreachable through typed code — the `CustomTypeOptions` augmentation makes a non-string key a compile error — so it needs an untyped dynamic key to trigger, which nothing in the tree produces yet. Worth a guard when the first surface resolves a key computed at runtime.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: `part()`'s `FORMAT_PART_MISSING` throw is unreachable, so its error string is unverified and a typo in it would ship.
  evidence: The branch requires an ICU release that stops emitting a `day`, `month`, `year`, `hour`, `minute` or `weekday` part for `hr` — not producible from test code without stubbing `Intl`, which the module's import-free constraint rules out. It is deliberate defensive totality rather than dead code, but it is the one named error in the module that no test names.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: The construction-argument scanner's `CONSTRUCTION_UNOPENED` branch is unreachable and `CONSTRUCTION_UNBALANCED` is untested, in a file whose detector self-tests are otherwise thorough.
  evidence: The needle ends immediately before `(`, so the unopened branch cannot be entered by any input the scanner accepts. The unbalanced branch is reachable in principle — a construction whose argument list never closes — but no self-test feeds it one, unlike every other shape in that suite.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: Nothing asserts the `SHAPES` formatter memoization, so deleting the cache would be silent.
  evidence: The two `Map`s are justified in the module header by "a month calendar formats several hundred cells from the same three shapes in one render", which is a performance claim no test holds. Removing both caches leaves every output byte-identical and the suite green. Nothing renders a calendar until Epic 3, so this becomes measurable when there is a surface to measure.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1c-localization-layer.md`
  summary: The test suite is unrunnable outside nvm: the foreign-timezone child requires Node's type stripping, which the machine's default Node does not support.
  evidence: `.nvmrc` pins 24.19.0 and root `package.json` declares `engines.node >= 22.13`, but the shell default here is v20.20.2, where the child exits on an unrecognized `--experimental-strip-types` flag. It fails loudly rather than silently, so it is not a coverage gap — but the failure names a Node flag rather than the version mismatch, which is a confusing first encounter. A version assertion in the test, or an engines check in the gate, would name the real cause.


## Deferred from: spec-1-1d-croatian-sign-in-shell (2026-09-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: The two-layout navigation shell — bottom tabs under 640 px, sidebar at or above — with its role-keyed destination table, the eight titled destination placeholder routes every link needs in order to typecheck, the nine `nav.*` resource strings, UX-DR41's 640–1024 collapsibility, and destination icons.
  evidence: Split from 1.1d at the token gate: the combined spec measured ~6,900 tokens against the 900–1600 standard, and the navigation shell is the second of the two independently shippable deliverables it carried (the sign-in screen renders no navigation, so neither breaks without the other). Human chose [S] on 2026-09-04. Specific findings already paid for and not to be re-derived: (a) TanStack Router 1.170.32 typechecks `<Link to>` against registered routes, so a navigable shell requires all eight destinations to exist — human chose titled placeholders, each rendering its own `nav.*` key as an `<h1>`, over blank or inert alternatives; (b) `apps/web/src/router.test.ts:25` asserts an exhaustive `routesById` list and must grow with every route, while `:39-40`'s `_notFound === true` assertion is preserved only by a `notFoundComponent`, never a catch-all route; (c) a pathless `_app` layout route is what keeps navigation off the sign-in screen; (d) the destination table must store translation KEYS and never labels — `const NAV = [{ label: 'Danas' }]` is not JSX and no syntactic ESLint rule can ever see it, which is the one L2 gap that cannot be closed by a selector; (e) `Sati` is ONE route with role-scoped content, not two destinations, decided by the human on 2026-09-04 against `EXPERIENCE.md:37` vs `:46`, so the admin set is the four member destinations plus `Raspored, Ljudi, Postavke rotacije, Organizacija` with `Sati` appearing exactly once; (f) binding order is member `Danas, Kalendar, Sati, Godišnji` (`EXPERIENCE.md:35-38`) then those four admin entries (`:42-46`), breakpoints `<640` / `640–1024` / `>1024` (`:149-153`), member sees no configuration surface (`:31`), roster is not a destination (`:70`); (g) no mockup exists for the navigation — `mockups/` holds only colour-themes, conflict-resolution, mobile-calendar and rotation-config, and icons are named for no destination in any artifact, so icon choice has no design source and `lucide-react@1.39.0` is the declared library; (h) collapsibility needs a toggle affordance, an accessible name and a decision on whether collapsed state persists — "nothing persisted" was decided for the theme, not for layout; (i) the active destination may not be signalled by colour alone (Q21/UX-DR37), so `aria-current="page"` plus a non-colour treatment; (j) 44 px touch targets on the tab bar; (k) `test/localization-applied.test.ts:167-187` will still hold `Danas, Kalendar, Godišnji, Raspored, Ljudi, Organizacija, Postavke, Smjena, Smjene, smjena` as words asserted ABSENT from the built chunk — the nav story is what makes eight of those legitimate and must re-derive that sweep the way 1.1d does for the five sign-in words; (l) `test/resource-hygiene.test.ts:120-143`'s reserved-vocabulary list will still ban `danas, kalendar, godišnji, raspored, ljudi, organizacija, postavke` and must be narrowed; (m) a `.tsx` test is silently not collected (AD-15, node environment, `*.test.ts` globs only), so the shell's correctness is assertable only at source/data level — which is precisely why the destination table is pure data.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: Dark `--sidebar-primary: oklch(0.488 0.243 264.376)` stays an unreconciled second blue, because the sign-in screen renders no sidebar and 1.1d no longer ships one.
  evidence: This entry supersedes the earlier one that parked the question until "the Epic 1 navigation shell" existed — that shell moved to its own spec at the 1.1d token split, so the trigger moved with it. The proposed resolution, costed but not applied: set `--sidebar-primary`/`--sidebar-primary-foreground` to the brand `--primary`/`--primary-foreground` in BOTH themes, so the application ships exactly one blue and the two themes stop disagreeing about whether the sidebar's primary is chromatic at all (light is currently the neutral `oklch(0.205 0 0)`, dark the saturated stock shadcn blue). Verified safe against the guards: `test/theme-fidelity.test.ts` derives its cases from DESIGN.md's front matter only and `--sidebar-primary` is a `BASE_TOKEN` (`test/theme-css.ts:40-69`), so a round-trip failure is not possible; `test/theme-tokens.test.ts` asserts token COUNTS (23 brand / 28 base), one declaration per theme and dark ≠ light, all of which survive since brand `--primary` differs between themes. What the change would owe: a new contrast pair asserting `sidebar-primary-foreground` on `sidebar-primary` at ≥ 4.5, since `BASE_PAIRS` (`test/theme-contrast.test.ts:40`) covers `sidebar` but not `sidebar-primary`. Note the alternative — dropping dark to the neutral the rest of the base uses — is also open, and DESIGN.md's `colors:` front matter mentions no sidebar token, so there is no recorded design intent either way. Decide when a surface first consumes the token; nothing does today.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: The organization logo lockup and the accent tint (UX-DR5) are unimplemented, and cannot be built until story 1.4 gives an organization a brand to render.
  evidence: UX-DR5 says an organization's accent may tint the application shell and the logo lockup only — never a shift state, a modifier, or `destructive` (`epics.md:113`) — and `epics.md:355` makes it an acceptance clause of story 1.4. Story 1.1d ships neither, deliberately and on the spec's frozen boundary ("No organization logo, lockup or accent tint — no organization exists; branding is 1.4's, and UX-DR5 goes with it"): the sign-in screen renders before any session, so there is no organization to read an accent from and no asset to render, and `ARCHITECTURE-SPINE.md` makes branding assets readable only within the owning organization. What 1.4 owes when it lands: a neutral fallback for "no logo" (the Epic 1 requirement says so explicitly), a tint that reaches the shell and the lockup and stops there, and an assertion that a shift fill, a modifier and `destructive` are all untouched by it. Note also that the theme layer defines no accent token at all today — DESIGN.md's `colors:` front matter has none — so 1.4 either adds one to `index.css` under the same measured-contrast rules the other 51 tokens follow, or resolves the accent at runtime from organization data, which is a different mechanism and needs deciding rather than assuming.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: The sign-in screen has no error state and no refused-save state, and whichever story adds them may not reach for `destructive`.
  evidence: 1.1d ships the form presentationally — no submit handler, no state, no validation, no error surface — because story 1.3 owns authentication and 1.1a's frozen boundary already assigned it there. So the three shapes a real sign-in produces are all unbuilt: wrong credentials, a deactivated account (story 1.6's sign-in block), and a network or function failure. UX-DR4 is the constraint that makes this worth recording rather than obvious: `destructive` is reserved EXCLUSIVELY for an unresolved conflict — not validation errors, not a refused save — so the error surface needs a signal it does not have a token for, and `test/theme-contrast.test.ts` measures `destructive` only against shift fills, never against a form. UX-DR34's voice rules bind the copy too: state the problem, keep every entered value, no exclamation mark. `apps/web/src/routes/prijava.test.ts` asserts `destructive` appears nowhere in the screen, so the story that adds the error state has to decide the signal deliberately — that assertion is the review moment, and it should not simply be deleted.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: `epics.md` allocates the UX design requirements to epics twice and the two lists disagree, so four UX-DRs are claimed by Epic 1 and assigned elsewhere.
  evidence: `epics.md:193` says "UX-DR1–2, 4–5, 31–32, 34, 40 land in Epic 1" and puts UX-DR3, 6 and 35 in Epic 2 and UX-DR36 in Epic 3. `epics.md:254`, Epic 1's own header, says "**UX:** UX-DR1–6, 31–32, 34–36, 40" — which claims UX-DR3 (contrast verified by measurement), UX-DR6 (ramp slots assigned in creation order), UX-DR35 (the six count categories) and UX-DR36 (the `Smjena`/`Tip smjene` terminology contract) for Epic 1 as well. Neither list is obviously wrong: UX-DR3 was in fact discharged in Epic 1 (`test/theme-contrast.test.ts` measures every pair), UX-DR6 is half-discharged (the ramp is numbered, but nothing is assigned to a slot until Epic 2), UX-DR35 is only two of six categories, and UX-DR36 is asserted negatively by `test/resource-hygiene.test.ts`'s `smjen` sweep. The risk is a requirement falling between the two readings — an epic retrospective checking the header list would call UX-DR35 done at two categories out of six, while one checking the allocation paragraph would not look at it in Epic 1 at all. Reconcile in `epics.md` before Epic 1's retrospective, and prefer the allocation paragraph as the source of truth or say why not.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: The four inherited shadcn primitives carry Tailwind v3 utility names, and the bare `shadow` utility is a silent no-op under Tailwind 4.
  evidence: `apps/web/components.json` declares `style: new-york` with `tailwind.config: ""`, and the registry serves that style's components with v3-era class strings; the four were inherited unmodified per DESIGN.md, which is the rule. Measured against the built stylesheet: every utility they use resolves except the bare `shadow`, which Tailwind 4 renamed — v4 emits `.shadow-sm` and `.shadow-xs` and no `.shadow`, so `Card`'s `shadow` and `Button`'s default-variant `shadow` generate nothing and both render flat. Cosmetic only, and nothing else is missing (`ring-1`, `ring-ring`, `space-y-1.5`, `file:border-0`, `peer-disabled:*`, `size-4` and the rest all resolve). Two resolutions, neither cheap enough for this story: re-inherit from the `new-york-v4` registry, which declares the unified `radix-ui` package plus `tw-animate-css` and so is an Ask-First dependency change; or hand-translate the shadow names, which breaks "inherited unmodified" and invites drift at the next `shadcn add`. Decide when the next primitive is added, because whichever way it goes should apply to all of them at once.

## Deferred from: code review of spec-1-1d-croatian-sign-in-shell (2026-09-04, iteration 1)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: Supersedes the "cosmetic only" claim about the Tailwind v3→v4 mismatch in the four inherited primitives. Two of the class names were RENAMED with different semantics, not merely dropped, and one of them removed the focus indicator in forced-colours modes.
  evidence: The earlier entry said the bare `shadow` utility was the only casualty and called the consequence cosmetic. Both halves were wrong. (a) `shadow` was renamed, not deleted: v4 emits `.shadow-xs`/`.shadow-sm` and no `.shadow`, so `Card` and the default `Button` variant render flat — cosmetic, as recorded, but the v4 name for the same visual weight is `shadow-sm`, so the fix is a rename rather than a re-inherit. (b) `outline-none` changed MEANING: in v3 it drew a transparent 2px outline that a forced-colours mode repaints into a visible one, and in v4 it emits `outline-style: none`, which nothing repaints. Combined with a focus ring drawn as `box-shadow` — which forced-colours modes drop entirely — the inherited primitives left a keyboard user in Windows High Contrast with no focus indicator at all. Fixed in `apps/web/src/index.css` with a `@media (forced-colors: active)` rule in `@layer base` giving `*:focus-visible` a real `outline`, `!important` because the utility layer comes after base and `outline-none` is a normal declaration. What remains open: the primitives still carry the v3 names, so the next `shadcn add` will produce a file written against v4 beside four written against v3, and the flat shadow is still there. The two resolutions are unchanged and both still Ask First — re-inherit from `new-york-v4` (which declares the unified `radix-ui` package plus `tw-animate-css`), or hand-translate the names and break "inherited unmodified". Decide when the next primitive is added, and apply it to all of them at once. Note the human decision of 2026-09-04: the mismatch is handled in our own CSS, not by re-inheriting.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: Both `--sidebar-primary` entries in this ledger remain open, and `--sidebar-ring` has now diverged from `--ring` as well.
  evidence: The ledger is append-only, so the earlier `--sidebar-primary` entry and the 1.1d entry that supersedes it both stand; the superseding one is the live version and the earlier is history, which is worth saying because a reader sweeping for "sidebar" finds two apparently competing recommendations. Added to the same question: story 1.1d's review moved light `--ring` from L 0.665 to 0.37 and dark from 0.556 to 0.8, because a ring at 0.665 measured 1.06:1 against the newly raised `--input` at 0.65 — the focus indicator had become invisible against the control it indicates. `--sidebar-ring` was deliberately NOT moved with it: it is measured against `--sidebar`, no surface consumes it, and no control abuts it, so there was no measurement to drive a value. The result is that the application now declares two visibly different focus-ring colours, and whichever spec first renders a sidebar control has to decide whether `--sidebar-ring` follows `--ring`, follows the same ring-versus-boundary rule against `--sidebar-border`, or stops existing. Owed at that point: a ring-versus-boundary contrast pair for the sidebar, the shape `test/theme-contrast.test.ts` now has for the page.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: The build ships a sourcemap containing the full original source — every Croatian string, every comment — to the CDN, and nothing decides whether it should.
  evidence: `apps/web/vite.config.ts` sets `build.sourcemap: true`, so `dist/assets/index-*.js.map` (~1.7 MB, larger than the chunk) is emitted and `test/static-hosting.test.ts`'s allowlist permits it because it lives under `assets`. That is a deploy decision nobody has made: it makes debugging a production incident possible, and it also publishes the whole client tree including comments that name unreleased stories, and it is why `test/localization-applied.test.ts`'s vocabulary sweep deliberately reads only `*.js` and not `*.js.map` — the map contains prose explaining why reserved words are absent, so sweeping it would report an offence for the explanation. Three options: keep it and say so in DEPLOY.md; switch to `sourcemap: 'hidden'`, which keeps the map for an error reporter but drops the `//# sourceMappingURL` comment; or upload maps to an error reporter and exclude them from the CDN artifact. Decide when story 1.3 adds the first real error path, since that is the first time a production stack trace is worth reading.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: No route declares an `errorComponent`, so a render-time throw anywhere under the root layout falls to TanStack Router's default error component.
  evidence: Story 1.1d closed the two blank-page routes it owned — an unknown path now renders `notFoundComponent`, and a failed localization boot leaves the static `index.html` fallback visible — but a component that THROWS during render is a third route to the same outcome and is handled by nobody. TanStack's default error component renders an untranslated English message with a stack trace, which fails L1/L2 and leaks source detail to a user. It cannot be fixed the way the other two were: the message has to come from `hr.json` and therefore from a working localization layer, so the fallback tree and the error tree are genuinely different problems. Deferred rather than done because the only throw reachable today is the `parseErrorHandler` case story 1.1c already routes to `⟦key⟧`, and because the copy for an unexpected-failure screen is undecided product vocabulary of exactly the kind this story was told to author in one reviewed place. Owed by whichever story first renders data: an `errorComponent` on the root route, its two or three `error.*` keys, and a decision on whether it may say anything more specific than "something failed" without leaking a code.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: `not-found.tsx` lives in `routes/` while being a component rather than a route, and `routes/README.md` says the directory holds "one file per destination".
  evidence: The spec named the path, and the reasoning for the component-not-route shape is sound and asserted (`router.test.ts` pins `notFoundComponent` by identity). But the file exports `NotFoundScreen` and registers no route, so `routes/` now contains something its own README does not describe, and the next author looking for the not-found screen has no reason to look there. Three options, none urgent: move it to `components/` and accept that a screen lives outside `routes/`; keep it and widen the README to say `routes/` holds route definitions plus the two components the root route composes; or fold it into `__root.tsx`, which is where it is registered and would make the pairing local at the cost of a longer file. Decide with the navigation shell, which introduces a pathless layout route and will have to say what `routes/` means anyway.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: Both screens hand-copy `CardTitle`'s classes onto an `<h1>`, so the heading style is duplicated and drifts from the primitive it was copied from.
  evidence: `prijava.tsx` and `not-found.tsx` each render `<h1 className="text-xl font-semibold leading-none tracking-tight">`. The reason is real — shadcn's `CardTitle` renders a `div`, and a screen's name is a heading, so using the primitive would cost the document its only `<h1>` — but the fix taken was to copy its classes, which means two files now encode a style the design system owns and neither will be updated when the primitive changes. Options: a small local `ScreenTitle` component in `components/`; `<CardTitle asChild>`, which the v4 primitives support but the v3 ones inherited here do not; or accept the duplication and assert the two strings are equal. Worth resolving when a third screen wants a heading, which is the navigation shell's eight titled destinations — each of which renders its `nav.*` key as an `<h1>`, so that story would triple the duplication.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: Story 1.3 must keep `/` resolving to something once its `beforeLoad` becomes conditional; a branch that falls through to no redirect renders the blank page 1.1d removed.
  evidence: `apps/web/src/routes/index.tsx` carries `beforeLoad` and deliberately NO `component`, because a component there would never render. That is correct while the function throws unconditionally, and it is a trap the moment 1.3 makes it conditional: `if (!session) throw redirect({ to: '/prijava' })` with nothing after it leaves a signed-in user on a route that renders nothing at all — the exact blank page at HTTP 200 that AD-14 makes possible and that this story existed to close. `apps/web/src/router.test.ts` asserts today that `/` registers no component, so 1.3 will have to edit that assertion, which is the review moment; this entry is what tells the author what the assertion was protecting. Either both branches throw a redirect, or the route gains a component and that assertion is replaced by one requiring it.

## Deferred from: code review of story-1-1d-croatian-sign-in-shell (2026-09-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1d-croatian-sign-in-shell.md`
  summary: The L2 eslint branch selectors require the literal's branch to be a direct child of the JSX expression container — a branch reached through an intervening non-branch node (a `.map()` callback's body, chiefly) bypasses the guard entirely, so a hard-coded Croatian literal inside a mapped ternary would ship undetected.
  evidence: Found by adversarial code review (edge-case-hunter layer). Confirmed by reading `eslint.config.js:169-233`: the branch selectors are anchored `${CHILD_CONTAINER} > ${BRANCH} > ${CONTENT_STRING}` and a double-branch variant, both requiring the branch at a fixed depth from the container. A widened selector using a descendant combinator (`${CHILD_CONTAINER} ${BRANCH} > ${CONTENT_STRING}`) was attempted during this review and closes the `.map()` gap, but it also matches `{t(cond ? 'a' : 'b')}` — the computed-key exemption `test/localization-guard.test.ts` explicitly protects as a negative control (`translatedComputedKey`), and the attempt was verified live to break that test. It was reverted. No selector achievable under this repo's syntax-only, non-type-aware ESLint config (`@babel/eslint-parser`, no `typescript-eslint` — see the TS7 entry above) can distinguish "a branch reached through a rendering callback" from "a branch reached through a `t()` call's argument" without semantic/type information. Not exploited today: this story's two screens render no lists. Reachable by the first list-rendering story — 1.5 (member list), 1.7/1.8 (teams), or a 3.x calendar view — worth a fresh look then, possibly with a selector scoped specifically to JSX-returning array-method callbacks (`.map`, `.flatMap`) rather than a general descendant sweep.

## Deferred from: spec-1-2-organization-provisioning (2026-09-04)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: `test/provisioning.test.ts` reports SKIPPED, not failed, when no database is reachable, so every rule story 1.2 owns is unenforced on any machine or pipeline without the local stack running. It is not load-bearing until CI runs it with a database attached.
  evidence: The file is this repository's first live-database test: it connects with `pg` to `SUPABASE_DB_URL` (defaulting to the CLI's fixed local `postgresql://postgres:postgres@127.0.0.1:54322/postgres`), probes the connection once at module scope with a 2 s timeout, and gates all twenty cases behind `it.skipIf(noDatabase)` — the `skipIf`-never-`return` rule `test/static-hosting.test.ts:13-15` sets, so a database-less checkout reports them as skipped instead of green having asserted nothing. The consequence is that `pnpm test` passes with the stack stopped while asserting nothing about Q6, Q3 or deny-all. `test/supabase-scaffold.test.ts` covers the source-text half unconditionally (0002 exists, exactly one `create constraint trigger`, it is `deferrable initially deferred` and fires `after delete or update`, no `create policy` anywhere, both tables enable RLS, no organization specific in any migration, and the two `enable_signup` values in `config.toml`), but the source text cannot prove behaviour — that a deferred trigger really permits an admin swap, or that `anon` really sees zero rows, is only observable against a running Postgres. This compounds the existing CI entry from spec 1.1a: whichever workflow lands must run `supabase start && supabase db reset` before `pnpm test`, and should fail rather than skip when the database is expected. Consider making the skip loud — an environment variable such as `SHIFT_REQUIRE_DATABASE=1` that turns the probe failure into a hard failure — so CI cannot silently regress to skipping.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: Generated database types have no home yet. When they arrive they belong in `apps/web/src/supabase/`, never in `packages/domain`, and nothing generates or checks them today.
  evidence: `organizations` and `members` now exist, so `supabase gen types typescript` would produce something for the first time, and story 1.3 is the first consumer — it needs typed reads through supabase-js. The destination is forced rather than chosen: `eslint.config.js:240-261` bans every `@supabase/*` import inside `packages/domain` and `packages/domain/test/purity.test.ts:101-108` forbids every bare import in `src`, so a generated file that imports supabase-js types cannot live there; AD-7 makes the domain package a leaf that depends on nothing, and a generated schema type is a data-access artifact. What has to be decided when it lands: whether the file is committed or generated at build time (committed is the only option that survives a build with no Docker, which is what Cloudflare Pages has), how it stays in step with a new migration (nothing enforces regeneration today), and whether a drift check belongs in the same CI job that runs `supabase db reset`. Note also that `apps/web/src/supabase/` does not exist yet — the architecture spine names it, 1.1a created `routes/`, `surfaces/`, `components/` and `i18n/`, and this is the story that would create it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: The `auth.users` insert is coupled to GoTrue's internal column expectations, in two places, and must be re-verified on every Supabase upgrade.
  evidence: `supabase/operator/provision-organization.sql` and `supabase/seed.sql` both write `auth.users` and `auth.identities` directly, because AD-17 forbids putting the secret key in an operator's hands and the Admin API is the only supported way to create a user. Nothing about those tables is a documented contract. Verified against CLI 2.116.0 / Postgres 17: only `id` is `not null` without a default, but `confirmation_token`, `recovery_token`, `email_change` and `email_change_token_new` are nullable columns GoTrue scans into non-nullable Go strings, and leaving any of them null makes every sign-in fail with `500 "Database error querying schema"` — an error that names none of them. A matching `auth.identities` row is also required (`provider 'email'`, `provider_id` = the user id as text, `identity_data` carrying `sub` and `email`), because a password grant resolves the account through identities rather than through `auth.users.email`. `test/provisioning.test.ts` asserts both — the four columns are non-null and one identity row exists — which turns a silent 500 into a failing test, but it cannot predict the *next* column GoTrue adds with the same shape. On each Supabase CLI or platform upgrade: run `pnpm exec supabase db reset`, provision a throwaway organization, and complete a real password grant against `/auth/v1/token`. The recipe is duplicated between the two files rather than shared, because the operator script must be a single prepared statement (`supabase db query` sends one) and cannot be included from the seed; a change to one is a change to both.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: A member signs in with a username alone, but the synthesized address story 1.2 issues is namespaced by the organization slug — so story 1.3 has to decide how a username becomes an address with no organization in hand.
  evidence: AD-12 says the app resolves a username to a non-routable synthesized address, and this story implements that as `<username>@<organization slug>.shift.invalid` (the shape the spec's Code Map names, and the shape that lets two organizations both issue the obvious username). `apps/web/src/routes/prijava.tsx` collects exactly two fields, `username` and `password`, and the sign-in screen renders before any session, so at the moment of sign-in there is no organization to build the domain from. Three ways out, none costless: make usernames globally unique and drop the slug from the address, which loses per-tenant namespacing and rewrites every issued address; add an organization field or a per-tenant sign-in URL to the screen, which changes 1.1d's frozen two-field form and its assertions; or resolve the username through an unauthenticated lookup — an RPC or a view over `auth.users` — which is a new read path that has to be safe to expose to anonymous callers and would leak the set of valid usernames. The slug is stored on `organizations` precisely so the address stays reproducible whichever way this goes (story 1.5 rebuilds it when issuing further credentials), but the decision is 1.3's and it is a blocking one.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: `ORGANIZATION_WOULD_HAVE_NO_ADMIN` travels in the error's MESSAGE, not its ERRCODE, because Postgres cannot carry an arbitrary word in `errcode` — so whatever reads the refusal at the edge must read `message`, and no other refusal may put prose there.
  evidence: The spec asks for a stable `SCREAMING_SNAKE` code raised "via `errcode`". Postgres refuses: `raise ... using errcode = 'ORGANIZATION_WOULD_HAVE_NO_ADMIN'` fails with `unrecognized exception condition`, verified against the running stack — `ERRCODE` accepts only a five-character SQLSTATE or one of Postgres's own condition names. The implementation therefore sets `errcode = 'check_violation'` (23514, the nearest true SQLSTATE for a deferred constraint trigger, and the one that makes PostgREST answer 400 rather than 500), puts the SCREAMING_SNAKE code in `MESSAGE`, and puts the single operand — the organization id — in `DETAIL`, so the raised error is still `{code, ...operands}` in AD-8's sense. `supabase/operator/provision-organization.sql` raises `ORGANIZATION_WITHOUT_ADMIN` the same way. What this owes the next story: an edge translator that maps `message` to a translation key rather than displaying it, a convention written down that a database refusal's `message` is a code and never a sentence, and a decision about whether a distinct custom SQLSTATE per refusal would be better than sharing 23514 once there is more than one — noting that PostgREST maps unknown SQLSTATEs to HTTP 500, which is why an invented class was not used here.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: `organizations` carries no branding, no hour bands and no leave-year semantics beyond a month and a day, and `members` carries no username column — all of which the stories that need them must add.
  evidence: Story 1.2 created the columns its own spec named (identity, Organization Type, timezone, locale, leave-year start) plus a `slug`, and stopped. The glossary's Organization additionally carries Hour Bands (their own table, CAP-3, Epic 2) and branding (story 1.4 — logo asset and accent, with the accent having no theme token today, already on this ledger). The Leave Year is stored as `leave_year_start_month` / `leave_year_start_day` with the day constrained to 1–28, so a leave year starting on the 29th, 30th or 31st is unrepresentable rather than validated — deliberate, because such a boundary has no February equivalent, but it is a product rule nobody explicitly decided and story 1.4's settings screen is where it becomes visible. `members` stores no username: the username is the local part of the synthesized address in `auth.users.email`, which means the member list cannot show or search usernames without joining `auth`, and story 1.5 will have to decide whether to add the column or expose it another way.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: Two concurrent transactions each deleting a different admin can both commit under READ COMMITTED, leaving an organization with zero admins — the exact state Q6 exists to prevent.
  evidence: `refuse_organization_with_no_admin()` runs at commit as a deferred constraint trigger and asks `exists(select 1 from members where organization_id = ... and role = 'admin')`. Under READ COMMITTED each transaction's check runs against a snapshot in which the other transaction's deletion is not yet visible, so an organization with exactly two admins can lose both: each sees the other's admin still present and commits. Nothing in the current tests can catch this — every case is single-connection. The fix is a row lock taken before the check (`perform 1 from organizations where id = ... for update`), which serialises the two commits so the second sees the first's effect; the cost is that every member delete or role change on one organization now serialises on its organization row, which at Q20 scale needs measuring rather than assuming. Worth resolving before story 1.5 makes member writes routine and before any bulk import exists.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: `truncate members` bypasses the zero-admins trigger entirely, because row-level triggers never fire for TRUNCATE.
  evidence: Postgres runs statement-level TRUNCATE triggers only; a `for each row` constraint trigger is not consulted, so `truncate members` would empty every organization with no refusal. Exposure today is low — TRUNCATE requires table ownership, and neither `anon` nor `authenticated` has it, so no PostgREST caller can reach it — which is why this is a note rather than a fix. It matters if a future migration, test helper or operator script reaches for TRUNCATE as a fast reset: the guarantee Q6 states unconditionally ("an organization can never be left with zero admins") is in fact conditional on nobody using that one statement. Either add a `before truncate ... for each statement` trigger that refuses, or write the limitation into the migration's comment so the next author is not surprised.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: The documented remote provisioning path is unproven and probably does not work — `db:provision` hard-codes `--local`, while DEPLOY.md tells the operator to run the same file with `--linked`, which routes over the Management API rather than libpq.
  evidence: `package.json`'s `db:provision` script passes `--local`, so the only exercised path is the local stack. DEPLOY.md's §7 tells an operator provisioning a real tenant to run the same file against a linked project, and `supabase db query --help` describes `--linked` as querying "the linked project's database via Management API" — an HTTP path, not a libpq connection. Every value the script reads arrives through `PGOPTIONS` session settings, which travel over the libpq startup packet; verified working against `--local` (multiple `-c` settings, values containing dashes, and via `-f`), but there is no reason to expect them to survive an HTTP transport, in which case every `current_setting` returns null and the script fails on the first `not null` column rather than doing damage. Unverified without a linked project. Resolve before the first real tenant is provisioned: either confirm `--linked` carries session settings, or document a different remote invocation (a direct connection string with `PGOPTIONS`, or `psql`, which is not currently installed on the operator machine).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: The first admin's password has no strength floor and reaches the database through a command line, so it lands in shell history and in `ps` output.
  evidence: Writing `encrypted_password` directly bypasses every GoTrue password policy, and `config.toml` sets none anyway (no `minimum_password_length`, no `password_requirements`), so the script accepts a one-character password for the account that administers an entire tenant; it only checks the value is non-empty. DEPLOY.md §7 puts the password in a `PGOPTIONS` assignment on the command line, where it is visible to `ps` for every other user on the machine, is written to shell history, and can be captured by statement logging. The escaping guidance covers spaces only, so a password containing a backslash or an equals sign breaks or is silently truncated. What is missing: a minimum length enforced in the script (raising a named code like the other refusals), a `read -s` recipe in the runbook so the value never appears in an argument list, and a stated path for how the password reaches the admin — noting that the accounts are `.invalid` addresses, so no email-based reset exists and an admin-issued reset is the only recovery.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-organization-provisioning.md`
  summary: `test/provisioning.test.ts` decides whether to skip by checking that Postgres accepts a connection, not that the schema is present — so a run during `supabase db reset` reports failures rather than skips.
  evidence: The gate is a 2-second connection probe. `supabase db reset` ends by restarting containers, and in that window Postgres accepts connections while the migrations have not been applied, so every case fails with `relation "organizations" does not exist` instead of skipping. Observed directly: a full `pnpm test` immediately after a reset produced four failures that vanished on re-run with no code change, and a second run produced one. This is a false-failure generator rather than a false-green, so it is less dangerous than the skip-when-absent gap already on this ledger — but it costs a debugging cycle every time, and it will be worse in CI where the reset and the test run are adjacent by construction. The probe should assert the schema exists (`to_regclass('public.organizations') is not null`), and treat connected-but-empty as "not ready" — or, better, wait for readiness rather than skipping, once CI owns the lifecycle.

## Deferred from: spec-1-3a-access-control-layer (2026-09-07)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: The SPA sign-in path — the browser Supabase client, the username-to-address module, the per-tenant `/prijava/$slug` route, the slug-less organization prompt at `/prijava`, `/`'s session-conditional `beforeLoad` with its signed-in placeholder, the new resource strings, and the router/screen test edits those force. Carries story 1.3's first acceptance criterion (a member with no email signs in by admin-issued username).
  evidence: Split from spec 1.3 at the token gate on 2026-09-07: the combined spec measured ~6,300–9,750 tokens against the 900–1600 standard, and the sign-in path is the second of two independently shippable deliverables (the access-control layer ships with no UI change; the sign-in path ships against policies that then already exist). Human chose [S], database layer first, matching the epic's stated dependency order. Decisions already made by the human on 2026-09-07 and NOT to be relitigated — (a) the organization slug rides in the URL as `/prijava/$slug`, so the credential form keeps EXACTLY its two frozen fields and the address is `username + '@' + slug + '.shift.invalid'`; chosen over global usernames, a third form field, and an anonymous username-resolution RPC (which would be an enumeration oracle); (b) a slug-less `/prijava` is a one-field organization prompt that navigates on, chosen because `/prijava/$slug` alone leaves THREE places with no slug in scope — `/`'s redirect target, bare `/prijava`, and `not-found.tsx:27`; (c) on success the user lands on `/`, which gains a minimal signed-in placeholder and NO navigation shell, which stays deferred. Findings already paid for: (d) `@supabase/supabase-js` is in no manifest and absent from `pnpm-lock.yaml` entirely — the version to match is `2.113.0`, the Edge Function's Deno specifier at `supabase/functions/admin-auth/index.ts:43`; (e) `apps/web/src/supabase/` EXISTS with a README (this ledger's earlier entry saying otherwise is wrong) specifying the client is built from `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` only, both already declared in `apps/web/.env.example` and typed non-optional at `apps/web/src/vite-env.d.ts:8-13` — so the runtime absence check is this story's and no new env var is needed; (f) the address concatenation must NOT live in the `.tsx`, because `apps/web/src/routes/prijava.test.ts:239`'s structural-attribute allowlist at `:92-105` makes `'@'` and `'.shift.invalid'` unsanctioned literals and the `from`/`params` attribute names are outside it too — and it must not live in `packages/domain`, whose bare-import allowlist is empty (`packages/domain/test/purity.test.ts:34`) and whose scope is the scheduling engine; (g) the frozen-boundary sweep at `prijava.test.ts:443-451` forbids the tokens `supabase`, `fetch(`, `useState`, `localStorage` and is self-labelled ":445-446 story 1.3 owns every one of these" — it must be NARROWED to what must still never appear, not deleted; (h) `prijava.test.ts:421-441`'s `onSubmit` regex requires the arrow body to contain no nested braces (`[^}]*`), so ANY real handler fails it and it must be replaced with an assertion about the real handler; (i) `prijava.test.ts:455-460` forbids `destructive` anywhere in a screen INCLUDING comments (UX-DR4 reserves it for an unresolved conflict), so the refused-sign-in signal must be authored without it and that assertion is the review moment, not a nuisance; (j) other assertions that must change: `prijava.test.ts:228` (exactly 2 `<Input>` + 1 `<Button>`), `:245` (exact key count of 5), `:257-259` (rendered set equals declared set, computed over `SCREENS` at `:49-52`), `:375-380` (`aria-describedby` is read as a SINGLE value, so a space-separated token list fails the resolving-id check); `apps/web/src/router.test.ts:33` (exhaustive `routesById`), `:41` (`match('/prijava')`), `:135` (redirect target pinned to exactly `/prijava`), `:149` (`/` must register NO component), and `:116`/`:130-131` (`beforeLoad` must ALWAYS throw, and the helper invokes it with `{}` as the entire context at `:120`, so a session-aware `beforeLoad` must tolerate that or the helper is rewritten) — while `:66-71` (`_notFound`) and `:101` (no `/$` route) must STAY green; (k) `apps/web/src/routes/index.tsx:21-25` warns that a conditional falling through to neither a redirect nor a component is the blank page 1.1d removed, so BOTH branches must resolve and `search: true`/`hash: true` must survive whichever runs; (l) new resource strings must clear THREE gates in the same commit — `test/resource-hygiene.test.ts:100` (`SANCTIONED_KEYS` is an EQUALITY assertion, list at `:42-50`), `prijava.test.ts:245`/`:257-259`, and the voice rules at `resource-hygiene.test.ts:141-199` (no `!`, no `smjen`, en dash for ranges) — and `'Nema'` and `'Odjava'` are asserted ABSENT from every built chunk at `test/localization-applied.test.ts:303-318`, so a natural error like `Nema takvog korisnika` FAILS; (m) `organizacija` is a reserved word banned at `resource-hygiene.test.ts:180-189` and `'Organizacija'` is asserted absent at `localization-applied.test.ts:303-318`, reserved for story 1.4's navigation destination — the organization-prompt screen needs it, and the sanctioned move (precedent recorded at `resource-hygiene.test.ts:175-179`, where 1.1d removed `prijava` and `lozinka` on earning them) is to drop it from the reserved list and move it from `NAVIGATION_AND_TERMINOLOGY` into `AUTHORED_VOCABULARY` at `localization-applied.test.ts:293-299` so it is held to an exact count instead, leaving `'Odjava'` and `'Nema'` banned; (n) any new source file must be added to `SOURCES` at `localization-applied.test.ts:48-57`, whose freshness guard FAILS rather than skips on a stale build; (o) `main.tsx:32-38` mounts `StrictMode > I18nextProvider > RouterProvider` with no query or auth provider, and `main.tsx` is unreachable from the node suite, so anything asserted about a new provider must be extracted the way `apps/web/src/i18n/boot.ts:26-36` was; (p) AD-15 bans jsdom in all three vitest configs and `.tsx` tests are not collected, so no assertion may render a component; (q) tokens are ES256-signed, so a test exchanges credentials at `/auth/v1/token?grant_type=password` and never forges one.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: AD-2's versioned Member active-status table is still owed by story 1.6, and 1.6 must **extend** `public.current_member_access()` rather than replace it. Today "active" means `auth.users.banned_until`, which is the authentication half of deactivation and nothing more.
  evidence: `supabase/migrations/0003_access_control.sql` reads active state as `u.banned_until is null or u.banned_until <= now()` joined from `auth.users`, chosen by the human on 2026-09-07 over shipping a half-met requirement or pulling 1.6's central deliverable forward. That satisfies "active state is read fresh on every policy evaluation" with no new column, no AD-2 violation and no encroachment on 1.6 — CAP-4 already words deactivation as "blocks authentication", and ban/unban is already `admin-auth`'s declared vocabulary. What it does NOT do is answer "was this member active on 2026-03-01": `banned_until` is current-state, it carries no validity range, and unbanning erases the fact that a ban existed. `test/provisioning.test.ts` asserts `members` never gains an `is_active`, `active` or `team_id` column, so 1.6's table is the only place that history can live. When 1.6 lands, the helper's active clause becomes `banned_until` AND the versioned table's current row — an account is active only if both say so — and the three freshness cases in `test/rls-isolation.test.ts` (`role and active state are re-read on the next statement`) are the ones that must be extended alongside it, including the expired-ban negative control which exists precisely because `banned_until is null` alone would make every lifted ban permanent.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `test/rls-isolation.test.ts` reports SKIPPED, not failed, when no database or no local API is reachable — so Q1 and Q2, the two security assertions every later epic is supposed to re-run, are unenforced on any machine or pipeline without the local stack running.
  evidence: The same gap the story-1.2 entry above records for `test/provisioning.test.ts`, and worse here because this file is the entire regression suite the epic names. It gates on two things: `noDatabase` (a 2 s `pg` connection probe at module scope) and `apiEndpoint === undefined` (derived by spawning `supabase status -o json`), combined as `noApi` for the 26 cases that speak HTTP. With the stack down, all 43 cases report skipped and `pnpm test` passes green having asserted nothing about tenant isolation or role refusal. The claims-injection half needs only Postgres; the PostgREST half additionally needs the API gateway and, for the hook cases, an auth container that was restarted after `[auth.hook.custom_access_token]` was added. Whichever CI workflow lands must run `supabase start` (not just `db reset` — see DEPLOY.md §1) before `pnpm test`, and should fail rather than skip when a database is expected; the `SHIFT_REQUIRE_DATABASE=1` idea on the 1.2 entry would cover both files at once.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: CORRECTION to the story-1.2 entry "Generated database types have no home yet" — its closing claim that `apps/web/src/supabase/` does not exist is wrong. It has held a README since story 1.1a. Everything else in that entry still stands: the commit-or-generate decision is still open, and 1.3a consumed no generated types.
  evidence: `apps/web/src/supabase/README.md` is tracked and dates from 1.1a; it specifies that the browser client is built from `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` only. The same correction is recorded in the spec-1-3a split entry above, finding (e). The 1.2 entry's substantive content is unaffected — the destination for generated types is still forced to that directory by `eslint.config.js:240-261` and `packages/domain/test/purity.test.ts:101-108`, and the open questions (committed vs generated at build time, how it stays in step with a new migration, whether a drift check joins the `db reset` CI job) are untouched. Story 1.3a needed no types at all: it is a migration plus two node-environment test files that reach the database with `pg` and PostgREST with `fetch`. The first real consumer is now the deferred sign-in story.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `revoke execute ... from public` does not actually make a function in `public` unreachable on Supabase — the platform's default privileges grant EXECUTE to `anon`, `authenticated` and `service_role` individually, so 0002's revoke on `refuse_organization_with_no_admin()` left all three holding it.
  evidence: Found while writing 0003's grants. Verified against the running stack: after `supabase db reset`, `select proacl from pg_proc where proname = 'refuse_organization_with_no_admin'` returns `{postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}` — the PUBLIC entry (`=X/postgres`) is gone exactly as 0002 intended, and three named grants that 0002 never wrote are present because `alter default privileges` in the Supabase image put them there. `test/provisioning.test.ts`'s `publicGrants` assertion counts only `entry::text like '=%'`, so it passes and always would have. Not exploitable today: Postgres refuses to call a trigger function directly ("trigger functions can only be called as triggers"), so `authenticated` executing it achieves nothing. 0003 works around it by naming every role in its revokes and then granting back only what a policy needs — `current_member_access` ends at `{postgres,authenticated}` and `custom_access_token_hook` at `{postgres,supabase_auth_admin}`, both asserted as exact lists in `test/provisioning.test.ts`. What is owed: the same treatment for 0002's function, as a new migration (0002 is forward-only and must not be edited), and a decision about whether every future `SECURITY DEFINER` function in this schema should carry the four-revoke boilerplate or whether the migration path should `alter default privileges` once for the `public` schema and stop inheriting the platform's.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: A policy expression's function call IS permission-checked against the querying role, so the RLS helper needs an explicit `grant execute ... to authenticated` — and that grant is now the single thing every policy in the system depends on.
  evidence: Verified against the running stack: with EXECUTE revoked from `authenticated`, `select count(*) from members` as `authenticated` fails with `ERROR: permission denied for function current_member_access` rather than returning zero rows. That is the opposite of the intuition that a policy is evaluated with the table owner's rights, and it means the grant is load-bearing rather than cosmetic. 0003 therefore writes it explicitly rather than inheriting Supabase's default privilege, on the same reasoning 0002:43-51 uses for `create extension pgcrypto` — a dependency that only works by accident fails on the first platform that does not share the accident. Two things follow for later stories: every organization-scoped table that copies this pattern reaches the same helper, so revoking that one grant breaks every read in the system at once (a `permission denied` error, not an empty result — loud, at least); and the failure mode is worth an assertion of its own if a future migration ever re-grants or re-revokes function privileges in bulk. `test/provisioning.test.ts` pins the exact grantee list, which is what would catch it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `organizations` has no write policy at all, so no admin can edit their own organization through PostgREST yet. That is story 1.4's, and 1.4 must add the policy alongside its settings surface.
  evidence: 0003 writes `organizations_select_own_organization` and nothing else, deliberately: 0002:12-17's argument is that a permissive policy written before the story that needs it stays invisible until nobody is looking, and the organization settings surface (identity, timezone, branding, leave year) is 1.4's deliverable. The consequence is that today an admin can read their organization row and change nothing on it — `PATCH /rest/v1/organizations` answers 204 having affected zero rows, the same silent shape 1.3a's tests assert for a refused member update. When 1.4 adds the write policy it must pin `id` in `with check` as well as `using` for the same reason the `members` write policies do, must require an active `admin` through `public.current_member_access()`, and must not open `insert` or `delete` at all: FR-2 says no product surface creates an organization, and the operator script in `supabase/operator/` is the only path.

## Deferred from: code review of spec-1-3a-access-control-layer (2026-09-07, iteration 1)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `custom_access_token_hook` is unguarded against four malformed-event shapes that GoTrue does not currently produce, and three of them would make every sign-in fail rather than fail closed.
  evidence: Raised by the edge-case layer and deliberately not patched, because each depends on an input the only caller never sends — which is exactly what makes it worth recording rather than fixing blind. (a) `event -> 'claims'` being JSON `null` rather than SQL null: `coalesce` does not substitute, and `jsonb_set`/`-` on a scalar raises. (b) `event ->> 'user_id'` present but not a valid uuid: `22P02` escapes the hook and GoTrue refuses the sign-in outright. (c) `event` itself SQL NULL, since the function is not `STRICT`: `jsonb_set` returns NULL and the hook answers null instead of a claims object. (d) An `organization_id` claim that is non-empty but not a uuid makes the policy cast raise `22P02` instead of failing closed with zero rows — unreachable today because the hook writes the claim from a uuid column and a signed ES256 token cannot be forged, but it is the one of the four that is a *policy* rather than a hook concern. A `jsonb_typeof` guard plus an `exception when others then return event` block would close (a)-(c); a safe-cast wrapper closes (d). Decide whether defence against a caller that cannot currently misbehave is worth the added surface, or whether a GoTrue upgrade is the trigger to revisit.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: The two live-database test files now duplicate eight helpers with no shared module, and their `FIXTURES` constants have already diverged in shape.
  evidence: `databaseUrl`, `reachable`/`noDatabase`, the `supabase status -o json` endpoint derivation, `connect`, `inRolledBackTransaction`, `refused`, `organizationId` and `FIXTURES` all exist in both `test/provisioning.test.ts` and `test/rls-isolation.test.ts`, some carrying comments that say "Copied from `test/provisioning.test.ts:124-148`". The two `FIXTURES` are already `{fixture, slug}` versus `{fixture, slug, admin, member, bystander}`, so the drift has begun. `test/theme-css.ts` establishes that this repo accepts a non-`.test.ts` shared module under `test/`, so the mechanism exists; 1.3a refactored *within* `provisioning.test.ts` while duplicating *across* files. This sits with the already-open ledger item about the comment-stripping constants and the recursive directory walker being duplicated across four test files with no shared home — the same problem, now with a second instance, and the two should be resolved together.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `custom_access_token_hook` re-implements the member lookup instead of deriving it from `current_member_access()`, so "how a member is resolved" now has two sites and only one is on the ledger for story 1.6.
  evidence: The hook runs its own `select m.organization_id from public.members m where m.auth_user_id = …`. The deferred entry telling 1.6 to **extend** `public.current_member_access()` when it adds the versioned active-status table says nothing about the hook, so a 1.6 change to member resolution would update the helper and silently leave the hook on the old definition. The duplication is not accidental — the hook runs as `supabase_auth_admin` at token-mint time and the helper runs as the querying role inside a policy, so sharing one function needs its grants thought through rather than assumed. Decide in 1.6 whether the hook calls the helper, whether both call a third private resolver, or whether the duplication is accepted and pinned by a test that fails when the two disagree.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `current_member_access()`'s comment justifies its own row-level-security exemption by table ownership, and a later `force row level security` would silently make that reasoning false and return zero rows from every policy in the system.
  evidence: The exemption on Supabase comes from the migration owner being `postgres` (which is `rolbypassrls`, verified) *and* owning both tables — not from ownership alone. `alter table members force row level security` is a plausible hardening step for a later story, and it removes the owner exemption specifically: the helper would then be subject to the very policies that call it, so every policy would evaluate against a helper that reads nothing. Nothing asserts the absence of `force row level security` in any migration, and the failure would present as a total, silent loss of access with a green static suite. A one-line source-text assertion in `test/supabase-scaffold.test.ts` would pin it; the comment should also name BYPASSRLS rather than ownership alone.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: Two live-database test files run in separate Vitest workers against one Postgres with no serialization, while one of them commits rows into the seeded fixture organizations and the other holds uncommitted locks on fixture `auth.users` rows.
  evidence: The root `vitest.config.ts` sets only `environment` and `include` — no `fileParallelism: false`, no `sequence` configuration — so `test/provisioning.test.ts` and `test/rls-isolation.test.ts` execute concurrently. `addThrowawayMember` commits members into the same fixture organizations that `provisioning.test.ts` counts, and the freshness cases hold open transactions carrying `update auth.users set banned_until` on fixture accounts while the other file reads them. Nothing documents or enforces an ordering. The plausible symptoms are lock waits, a flake that appears only under load, or fixture drift left behind when a worker crash or `--bail` skips `afterAll` — and the vacuous-pass guards are supersets, so they would not catch the drift. Decide between `fileParallelism: false` for the root project, a per-file advisory lock, or giving each file its own throwaway organization so the two never touch the same rows.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `functionSecurity`'s exact-grantee capability is unused at the one call site where it would currently find a real defect, so the corrective migration for 0002's function has no test that will flip when it lands.
  evidence: The refactor gave `functionSecurity` a `grantees` list and applied exact-list assertions to both new functions, but `refuse_organization_with_no_admin()` still goes through `expectRunsAsOwner`, which checks only `publicGrants` — and `publicGrants` counts `entry::text like '=%'`, which is blind to per-role grants. Per this story's own ledger entry, that function's real ACL is `{postgres,anon,authenticated,service_role}`, so the exact-list assertion would fail today, which is why it was not applied. Assert the current (wrong) grantee list with a comment naming it as the known-bad state, so the migration that fixes it turns the test red and forces the update — otherwise the fix can land with nothing observing it, and the shared helper's most discriminating output stays unused precisely where it matters.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: A write policy's own predicate cannot be asserted with a filtered statement, because PostgreSQL applies the SELECT policy to an `update`/`delete` that reads any column — so `... where id = $1` is refused by the read policy and proves nothing about the write policy. Every later story copying this pattern needs the column-free form to test its write predicate.
  evidence: Found while re-verifying review finding 2 by mutation. With `and access.is_active` dropped from all three `members` write policies, `update members set leave_allowance_days = leave_allowance_days + 1 where id = $1` as a banned admin still affects zero rows — the `where` on `id` and the `leave_allowance_days + 1` expression both read columns, which pulls in `members_select_own_organization`, which carries its own `is_active`. The same mutation with `update members set leave_allowance_days = 7` (no `where`, constant on the right of `set`) updates every own-organization row, and `delete from members` (no `where`) deletes all of them. Verified live in both directions. `test/rls-isolation.test.ts`'s banned-admin update/delete case therefore uses the unfiltered form and says why; the insert case needs no such care, because insert reads nothing. Two consequences: PostgREST cannot issue an unfiltered write, so that case is claims-injection only and there is no HTTP-level equivalent to add; and the practical security property under that mutation still holds today only because the read policy also gates it — which is exactly the defence-in-depth that would disappear the moment a story loosens a read for a surface whose writes were never re-reviewed. Story 1.4 (organizations write policy, which the migration says will be built by copying this one), 1.5 and 1.6 each need one column-free case per write policy they add, or their write predicates are untested.

## Deferred from: code review of spec-1-3a-access-control-layer (2026-09-07)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: `refuse_organization_with_no_admin` still carries the default `anon`, `authenticated` and `service_role` EXECUTE grants, while the two functions 0003 adds revoke all three.
  evidence: Confirmed live against the running stack — `pg_proc.proacl` reads `{postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}` for `refuse_organization_with_no_admin`, against `{postgres=X/postgres,authenticated=X/postgres}` for `current_member_access` and `{postgres=X/postgres,supabase_auth_admin=X/postgres}` for `custom_access_token_hook`. 0002 revoked only from PUBLIC, and 0003's own comment explains why that is insufficient: "Supabase's default privileges grant EXECUTE on a new function in `public` to `anon`, `authenticated` and `service_role` individually: revoking PUBLIC alone would leave all three, and the two request roles are precisely the ones a token arrives as." That argument applies verbatim to the older function. Exposure is low — it is a trigger function, and invoking it outside trigger context errors rather than acting — so this is posture consistency rather than a reachable hole. The 1.3a review refactor routed it through `expectRunsAsOwner()` (which checks PUBLIC only) and pointedly did not add it to the new exact-grantee list, which is what makes the inconsistency deliberate-looking rather than overlooked.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: The claim-plus-helper predicate is written out six times across the five policies, with nothing comparing the policies to each other.
  evidence: `0003_access_control.sql:250, 265, 282, 307, 316, 334` each repeat the same six-line shape — `nullif(((select auth.jwt()) ->> 'organization_id'), '')::uuid` plus a `current_member_access()` subquery, twice inside `members_update_by_own_active_admin`. The story's own review round already proved that dropping `and access.is_active` from the three write policies and from `organizations_select_own_organization` is invisible to the suite, and that the delete policy could be removed whole. Those specific mutations are now covered, but the structural cause is not: a single-policy drift is a local edit with no comparative guard. Two small STABLE helpers (a tenant matcher and an active-admin predicate) would collapse roughly forty lines and give each rule one definition. Note the migration names these policies as the template story 1.4 copies for `organizations`, so drift propagates by design. A related design option recorded at the same time: having the helper return *no row* for an inactive caller — or adding a second `public.active_member_organization()` — would make the dropped-`is_active` mistake unrepresentable rather than merely tested.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: The zero-admins constraint trigger has never been exercised through a policy-bound session, only as the bypassing owner.
  evidence: `0003_access_control.sql` reasons explicitly about the interaction — "an admin deleting the last admin of their own organization is refused by that trigger at commit, not here" — but every trigger case in `test/provisioning.test.ts:522-528` and its neighbours runs on a bare `postgres` connection, which is `rolbypassrls`. So the composition of a SECURITY DEFINER constraint trigger with the new delete policy is unasserted, and so is the shape a caller actually sees over PostgREST when the trigger fires (the story established that RLS refuses `delete` silently with 204, while the trigger raises `check_violation` at commit — two different refusal shapes reaching the same caller). Worth a case when story 1.6 makes deactivation real, since that is the first story where an admin can legitimately reach the last-admin boundary.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3a-access-control-layer.md`
  summary: The source-text matchers `migrationStatements()` and `policyBody()` are fragile in ways that fail open rather than red.
  evidence: `test/supabase-scaffold.test.ts:230-240`. `migrationStatements()` applies `/--[^\n]*/g` across the whole file including dollar-quoted function bodies and string literals, so any future migration containing `--` inside a literal silently loses the rest of that line before assertions run; `policyBody(name)` matches `create policy <name>\b[\s\S]*?;` and therefore truncates at the first semicolon, which a semicolon inside a literal would move. Both then make their assertions pass or fail for a reason unrelated to the policy. Anchoring comment-stripping to line starts fixes the first cheaply; the durable fix for the second is asserting against `pg_policies` / `pg_get_expr` in the database-attached suite instead, which is the same direction as the open item about giving the DB-independent suite an exact policy-name list.

## Deferred from: build review of spec-1-3b-spa-sign-in-path (2026-09-08)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: A signed-in visitor can open `/prijava` or `/prijava/$slug` and sign in over a live session, and with no sign-out affordance anywhere there is no way to end a session at all.
  evidence: Neither sign-in route guards on an existing session, so a member signed in to organization A can open `/prijava/zastita-split`; on failure they stay signed in as A and `/` renders the placeholder as though nothing happened. The guard itself is three lines (`beforeLoad` redirecting to `/` when `currentSession()` is non-null), but it is only half the problem: `Odjava` is a reserved word held for the navigation shell, so this story deliberately shipped no sign-out. In a shared-device shift-work context — the actual deployment shape — that is the more serious half. Resolve both together in the story that earns `Odjava`, rather than adding a guard that makes the missing affordance harder to notice.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: The deep-link search and hash are preserved for exactly one hop and then lost, and the bounced visitor also loses the organization they were on.
  evidence: `index.tsx`'s redirect carries `search: true` / `hash: true` onto `/prijava` — defended at length in that file — and nothing forwards them to `/prijava/$slug`, while `submit()` ends with `navigate({ to: '/' })` carrying neither. So `/?invite=abc#section` survives the redirect and dies at the next navigation, which makes the careful preservation decorative. The same redirect sends the visitor to the bare prompt rather than the slug they were on, so an expired session means retyping it. Both want the same fix — a redirect-back target threaded through the sign-in path — which is why they are one entry. No route declares a search schema yet, so nothing is broken today; this becomes real with the first invite or deep link.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: The refusal message has no visual error affordance and is rendered in the same neutral treatment as the static password-reset guidance directly below it.
  evidence: UX-DR4 reserves `destructive` exclusively for an unresolved conflict and `test/theme-contrast.test.ts` measures it only against shift fills, so the story correctly authored the error without it — but nothing was authored in its place. The message lands on `border-input px-3 py-2 text-sm font-medium`, visually identical to `auth.passwordReset` sitting beneath it, so the one piece of state-dependent text on the screen reads as more static help. Options that stay inside UX-DR4: an icon, a tonal background from the existing brand ramp, or spacing that separates state from guidance. Needs a design decision and possibly a new token pair with its own contrast assertion, which is why it is not a patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: No focus management or busy state around a refused sign-in, and `disabled={pending}` drops keyboard focus to `<body>` mid-flow.
  evidence: On refusal nothing moves focus to the message, so a screen-reader user is not told the submission failed unless they navigate back to it; `aria-live` is absent. Separately, disabling the submit button the user has just activated removes the focused element from the tab order, which drops focus to the document body — the keyboard user loses their position at exactly the moment they need to correct a field. UX-DR34's "state the fact rather than hide the affordance" also argues against silently disabling the only control while saying nothing; a pending label would need a new resource key. Wants a considered accessibility pass across every form the app grows, not a local fix on one screen.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: `/`'s `beforeLoad` is now async and may perform a network token refresh, with no pending component configured anywhere.
  evidence: Every resolution of `/` awaits `getSession()`, which supabase-js may satisfy by refreshing the token over the network. Neither `createRouter` nor the root route declares `pendingComponent` or `defaultPendingMs`, so the post-sign-in navigation can sit on a blank frame for the duration on a slow connection. The awaited-reader design is right and well argued in `__root.tsx` — reading the session fresh on every resolution is what makes a stale snapshot impossible — but its latency consequence was never addressed. Decide alongside the first route that loads data, since the answer (a shared pending treatment) should not be invented twice.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: The organization prompt refuses a malformed slug as inertly as an empty one, giving a button that visibly does nothing.
  evidence: `submit()` returns on `slug === null`, and `required` covers only the empty case, so a typed `dvd_kastel` or a 70-character paste is indistinguishable from a broken page. No `hr.json` key carries a format hint and `auth.organization.label` is just `Kratica organizacije`, so nothing tells the user what shape the field wants. The spec's I/O matrix says "Empty input refused inertly" and is silent on malformed, which is why the implementation is not wrong — but the file's own doc comment justifies the inertness with the enumeration-oracle argument, and that argument does not reach here: telling somebody their input is not a well-formed DNS label discloses nothing about which organizations exist. Wants a copy decision (a hint, or a format message) rather than a code change.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: `SCREENS` and `SOURCES` are two hand-maintained lists of the same `.tsx` files, and both carry comments saying a file missing from them is swept by nothing.
  evidence: `apps/web/src/routes/prijava.test.ts` and `test/localization-applied.test.ts` each gained the same two entries in this story. Both files document the hazard — a screen absent from the list is measured by no sweep, and in `localization-applied.test.ts`'s case the freshness guard also stops covering it — and neither closes it. A glob over `apps/web/src/routes/*.tsx` would make the next screen covered by default and turn the omission into an impossibility rather than a convention. The reason to defer rather than patch: `SOURCES` is an mtime list where a glob changes the freshness semantics, so the two lists may not want the same treatment, and getting that wrong weakens a guard that is currently strict.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: The reserved-vocabulary sweep matches exact substrings, so any inflected form of a reserved word ships without reaching the review moment the list exists to create.
  evidence: `test/resource-hygiene.test.ts` lowercases each message and applies `.toContain(word)`. Story 1.3b's `Kratica organizacije` cleared the old ban only because `organizacije` does not contain `organizacija` — which the story's own new comment observes and leaves open. The same hole admits `odjave`, `postavki` and every other Croatian inflection of the seven words still reserved, each of which is exactly the vocabulary the list is protecting for a later story. Stemming to prefixes (`organizacij`, `odjav`, `postav`) would close it, at the cost of false positives on unrelated words sharing a stem — which is the judgement call that makes this a decision rather than a patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: `createClient` is called with no `Database` generic and no explicit auth options, so every PostgREST call in later stories starts untyped.
  evidence: `apps/web/src/supabase/README.md` still opens with "The generated database types and the single browser Supabase client", but no types file exists and the client is a bare `SupabaseClient`. This is the consumer side of the already-open ledger entry about whether generated types are committed or built — story 1.3b is the first story that would have used them and did not, which is the fact that entry was waiting for. Separately, `persistSession`, `autoRefreshToken`, `storageKey` and `detectSessionInUrl` are all inherited defaults; the spec's "Ask First" gate names session persistence explicitly, so the defaults deserve to be recorded as a decision rather than inherited silently. Resolve with the generated-types entry, since both are answered by the same edit.

## Deferred from: code review of spec-1-3b-spa-sign-in-path (2026-09-08)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: Redirect an already-signed-in visitor away from `/prijava` and `/prijava/$slug`.
  evidence: Neither sign-in route reads the session in `beforeLoad`, so a signed-in person reaching either screen is offered a credential form. It is not a security hole — a new sign-in simply replaces the session — but there is no sign-out affordance anywhere (`Odjava` stays reserved and the navigation shell is itself deferred), so re-authenticating is the only thing the screen can do for them. Belongs with the story that introduces sign-out, because the guard and the affordance are the same decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: Narrow `STRUCTURAL_EXPRESSION` to an explicit subset rather than generating it from the whole `STRUCTURAL_ATTRIBUTES` set.
  evidence: Only `aria-describedby` needed to become expression-legal in story 1.3b, but `prijava.test.ts:160` builds the expression matcher from every entry in `STRUCTURAL_ATTRIBUTES`, so each present and future entry is exempted in both quoted and expression syntax at once. The deliberate-widening review moment happens only for the quoted form; the next attribute added to that list — a `title`, an `alt`, a `label` — is silently exempted in expression form with no such moment. No current defect: every present entry is genuinely structural.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: Move the shared `SESSION` test fixture into one module.
  evidence: `router.test.ts`, `client.test.ts` and `sign-in.test.ts` each declare `const SESSION = { access_token: 'token', user: { id: 'member' } } as unknown as Session;` with near-identical comments. Three copies of one shape is exactly what drifts — one file gaining a field the others lack — and the drift would be invisible because each suite passes against its own copy.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: Decide the browser client's auth options, and stop the node suite from holding a real auto-refresh timer.
  evidence: `client.ts:116` calls `createClient(url, publishableKey)` with no `auth` options, so `autoRefreshToken` and `persistSession` take library defaults. `client.test.ts`'s "returns the same instance every time" therefore constructs a REAL `SupabaseClient` in the node suite; the module-level memo is never torn down and nothing calls `stopAutoRefresh`, so a test run can be left holding a background refresh timer. Pairs with the already-open question of which client options this application actually wants.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3b-spa-sign-in-path.md`
  summary: Decide whether `iceberg-js@0.8.1` belongs in the browser bundle's dependency graph.
  evidence: The spec's Verification section expected `pnpm install` to add `@supabase/supabase-js@2.113.0` "and nothing else"; the lockfile gained `iceberg-js@0.8.1` and `tslib@2.8.1` as new package entries. `iceberg-js` is a table-format library reachable via `storage-js`, for a story — and an application — that makes no storage call. Transitive rather than declared, so the "Ask First" dependency gate was not bypassed, but it widens the supply-chain surface of a bundle whose central invariant is key containment. Verify whether it is tree-shaken from the production build before deciding.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: The navigation shell's chrome and its exit — the bottom tab bar below 640 px, the sidebar at and above it with 640–1024 collapsibility, destination icons, the non-colour active-destination treatment, `signOut`, and the already-signed-in redirect on both sign-in routes.
  evidence: Split from the navigation-shell spec at step-02's token gate on 2026-09-08. The combined spec measured ~2,900–3,800 tokens against the 900–1600 standard — roughly the same overrun that forced the 1.1a/1.1b and 1.3a/1.3b splits. Human chose [S], skeleton first. The seam was chosen because it is the only one available: the route skeleton and the vocabulary-gate migration are one atom (the moment eight screens and nine `nav.*` keys exist, every gate fires at once), and the chrome cannot precede the skeleton because TanStack Router 1.170.32 typechecks `<Link to>` against registered routes. Two alternative seams were rejected — splitting the two layouts contradicts UX-DR31's "two layouts, one architecture", and splitting by role would tear the role-keyed destination table in half. Recorded cost of this seam, weaker than the previous two splits: part A ships eight destinations reachable only by typing a URL, so it delivers no user-visible value alone, unlike 1.1a (infrastructure) and 1.3a (database) which each stood up on their own.
  Findings already paid for and NOT to be re-derived — (a) `lucide-react@1.39.0` IS installed and `apps/web/components.json` declares `"iconLibrary": "lucide"`, so no dependency is needed, but NO artifact names an icon for any destination (`mockups/` holds only colour-themes, conflict-resolution, mobile-calendar and rotation-config), so icon choice has no design source and remains a human decision; (b) `apps/web/src/components/ui/` holds ONLY `button.tsx`, `card.tsx`, `input.tsx`, `label.tsx` — there is no Sheet, Sidebar, Tabs, Tooltip or Separator, so every primitive the chrome needs must be scaffolded; (c) the eight `--sidebar-*` tokens already exist in both themes (`apps/web/src/index.css:102-109` light, `:160-167` dark) and were evidently added in anticipation of shadcn's Sidebar primitive that was never installed; (d) `--sidebar-primary` is asymmetric — neutral `oklch(0.205 0 0)` in light, chromatic `oklch(0.488 0.243 264.376)` in dark — which is the open ledger decision at this file's earlier entries, and it triggers the moment the active-destination treatment consumes the token; (e) `test/theme-contrast.test.ts:40`'s `BASE_PAIRS` covers `sidebar` but NOT `sidebar-primary`, so a fill built from that token owes a new contrast pair at AA_BODY 4.5; (f) `test/theme-css.ts:40-69` hard-codes `BASE_TOKENS`/`BRAND_TOKENS` and `test/theme-tokens.test.ts:34-40` asserts counts of 23 and 28 against those arrays rather than against `index.css`, so a new token added to the stylesheet but not to the arrays is silently untested rather than red; (g) responsive breakpoints are greenfield — the only `sm:`/`md:`/`lg:` anywhere in `apps/web/src` is a stock `md:text-sm` at `components/ui/input.tsx:11` — so there is no in-repo pattern to follow; (h) the 44 px floor is real and mutation-proven at `apps/web/src/routes/prijava.test.ts:106` (`TARGET_FLOOR_PX = 44`), `:480-505` and `:1163-1167` (`heightPx` understands `h-11`, `h-[44px]`, `h-[2.75rem]`), the inherited primitives are `h-9`/`h-10` and every screen composes `h-11` on top — so each navigation control must do the same and `SCREENS` at `prijava.test.ts:68-78` must carry an exact `expectedControls` count for both chrome components, since the comment at `:69-73` records that a loop with no count behind it read as coverage while asserting nothing; (i) `signOut` must copy `signIn`'s injection shape — `sign-in.ts:135` takes `auth: PasswordAuth` as its FIRST parameter and `:78-83` defines that as a two-line structural interface, with the rationale at `:24-29` being that the client as a parameter is what puts the whole outcome matrix in the node suite despite AD-15 banning jsdom; no `signOut` wrapper exists anywhere in the repo today, only `supabaseClient().auth` at `client.ts:155-163`; (j) the already-signed-in redirect is three lines (`beforeLoad` redirecting to `/` when `currentSession()` is non-null) and this ledger's entries at the `prijava.tsx:215` and `router.test.ts` `SESSION`-fixture items both concluded independently that the guard and the sign-out affordance are ONE decision, because a guard added without an exit makes the missing affordance harder to notice rather than easier — in a shared-device shift-work context that is the more serious half; (k) `Odjava` is banned in BOTH gates and part A does not earn it if part A ships no sign-out — it stays in `test/resource-hygiene.test.ts`'s reserved array and in `test/localization-applied.test.ts`'s `NAVIGATION_AND_TERMINOLOGY` until this story lands, which means part A's reserved list is NOT emptied after all and the "empty array asserts nothing" trap belongs to THIS story instead; (l) collapsibility needs a toggle affordance, an accessible name, and a decision on whether collapsed state persists — "nothing persisted" was decided for the THEME, not for layout; (m) the active destination may not be signalled by colour alone (Q21, UX-DR37), so `aria-current="page"` plus a non-colour treatment; (n) the binding source for 640–1024 collapsibility is `EXPERIENCE.md`'s Responsive & Platform table and UX-DR31/UX-DR32 at `epics.md:151-152` — NOT UX-DR41, which this ledger's earlier entry miscited and which `epics.md:163` shows is about the calendar at each breakpoint and says nothing about navigation.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: The signed-out redirect discards the destination the visitor asked for, which makes its `search`/`hash` preservation theatre.
  evidence: `_app.tsx` throws `redirect({ to: '/prijava', search: true, hash: true })`, and both the comment and the test argue those values would otherwise be "unrecoverable" — but the PATHNAME is dropped, and `prijava.tsx` navigates to `/` after a successful sign-in (`prijava.test.ts`: "lands a signed-in visitor on / and nowhere else"). So `/kalendar?tim=2#tjedan` becomes `/prijava?tim=2#tjedan`, carries `tim=2` onto a screen with no use for it, and discards it at `/`. The parameters are unrecoverable either way, so the preservation currently buys nothing while reading as though it buys something. The fix is a return path (a `redirect` search param on `/prijava` consumed after sign-in), which touches `prijava.tsx` — a file this spec deliberately put behind Ask First. Pairs naturally with part B, which already owns the already-signed-in guard on the same routes.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: The six earned navigation words lose always-run coverage: they leave `resource-hygiene`'s unconditional `reserved` sweep for a count sweep that is `skipIf(notBuilt)`.
  evidence: `test/resource-hygiene.test.ts` reads `hr.json` directly and always runs; `test/localization-applied.test.ts:406,413` are both `it.skipIf(notBuilt)` with `notBuilt = !existsSync(apps/web/dist/assets)`. Root `package.json` runs `"test": "pnpm -r test && vitest run"` with no `pretest` and no build, and there is no CI workflow, so on a clean clone every one of those assertions reports skipped. Moving a word between two build-conditional lists changes nothing, but REMOVING it from `reserved` removes an unconditional guard — that removal is correct (the word is now legitimately in `hr.json`), yet what replaces it for the hard-coded-literal case is build-conditional. Residual exposure is narrow: ESLint L2 still catches JSX-text and guarded-attribute shapes unconditionally (`eslint.config.js:176-220`), so only the non-JSX shapes on an unbuilt checkout are uncovered. The underlying condition is already ledgered above ("Two build-output tests additionally skip when `dist` is absent") and the CI entry would close it; this records that the navigation story adds new reliance on it.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: The ~20-line session-guard body is duplicated verbatim between `_app.tsx` and `index.tsx` with nothing enforcing the two stay identical.
  evidence: The `let session: Session | null = null`, the `try`/`catch`, `console.error(SESSION_UNRESOLVED, cause)`, the `session !== null` early return and the identical `redirect({ to: '/prijava', search: true, hash: true })` are copied between the two files. The spec's Design Notes accepted duplication but described it as "two call sites of a four-line guard" — the real body is about twenty lines, so the spec understated what it was authorising. `_app.tsx`'s own comment says "two guards that differ are two guards to reason about", and nothing asserts they do not differ. Extracting `requireSession(context)` would fix it but edits `index.tsx`, which this spec put behind Ask First; do it in the story that next touches that file.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: The eight destination route files are copy-paste, including a twenty-line docblock repeated verbatim eight times.
  evidence: Roughly 200 of the diff's lines are the same file with one word changed, and the `<main>`/`<h1>` class strings are duplicated eight times plus a ninth in `index.tsx`. The direct cost was already paid once in this very story: a stale docblock claiming the deep link resolves to `['__root__']` had to be corrected in every copy. A `createDestinationRoute(path, key)` factory or one shared placeholder component collapses it. Deferred rather than patched because part B rewrites these screens anyway and a refactor now would be discarded.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: One `/sati` route must eventually serve two distinct surfaces and no mechanism is recorded anywhere.
  evidence: `EXPERIENCE.md`'s surface inventory lists "My hours" (CAP-14, UJ-2) and "Organization hours" (CAP-14, UJ-4) as separate surfaces with separate journeys, and UX-DR32 groups the admin's `Sati` with configuration. The 2026-09-04 human decision collapsed them to ONE destination, which settles the count but not how one route renders two surfaces — the calendar's precedent is two modes (`Moj raspored` / `Sve smjene`), which may or may not be the intended shape here. `sati.tsx` says only that content is role-scoped. This is the one place the eight-not-nine collapse leaves real design work undone, and it lands on Epic 4.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: A third copy of the resource-file flattener now exists, across three test files.
  evidence: `destinations.test.ts` defines `resourceKeys`/`resourceMessages`, duplicating `leafKeys`/`messages` in `test/resource-hygiene.test.ts` and the flattener `prijava.test.ts` already self-tests. Three implementations, three self-tests, three ways to drift apart. The `destinations.test.ts` copy also uses a default-parameter-reads-the-file idiom that re-reads and re-parses `hr.json` on every call and conflates a pure recursion with an I/O entry point.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: A `currentSession()` that never settles leaves `beforeLoad` unresolved — a blank page at HTTP 200 with no timeout anywhere.
  evidence: Both `_app.tsx` and `index.tsx` `await` the reader with a `try`/`catch` that handles rejection but not non-settlement; `getSession` can hang where storage is blocked or `navigator.locks` stalls. `__root.tsx` registers no `errorComponent`, so nothing catches it downstream. Pre-existing in shape — `index.tsx` shipped with it in story 1.3b — so this is not the navigation story's defect, but the shell doubles the number of routes exposed to it.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: Route-level role gating, deactivated accounts and expired sessions are all unhandled at the layout; due when the first destination holds admin data.
  evidence: `_app.tsx`'s guard asks only whether a session exists, so a member-role account typing `/organizacija`, a deactivated account whose row says otherwise, and a session `getSession` returned but could not refresh all reach every destination. Correct today and deliberately so — AD-10 puts role and active-status enforcement in database policies read fresh on every query, the destination table owns visibility, and these eight screens render a heading and nothing else — so there is no exposure while they hold no data. The patch pass pinned the session-only decision with an assertion so a later change has to be deliberate. What is deferred is the decision itself, which comes due the moment a destination renders anything an admin owns.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: `destinationsFor` with a role outside the `MemberRole` union yields an empty navigation silently, with nothing asserting that is intended.
  evidence: `MemberRole` is a closed union so today this is a compile-time error, and no shipped module calls `destinationsFor` yet. Part B feeds it a role read from the database, where the value arrives as text at runtime and the union is an assumption rather than a guarantee. An unrecognised role currently filters to zero destinations, which renders as navigation with no entries rather than as an error — indistinguishable from a member with no access, and there is no test or comment recording which of those two behaviours is wanted.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-a-route-skeleton.md`
  summary: `SOURCES` in `test/localization-applied.test.ts` stays hand-maintained while its own comment says an omission is guarded by nothing.
  evidence: The comment reads "A `.tsx` carrying a string that is absent from this list is swept by nothing — the freshness guard would not notice a build that predates it." This story added nine paths by hand and nothing cross-checks the list against `apps/web/src/routes/*.tsx`, so a tenth route file added later is exactly the case the comment describes. Globbing the routes directory and asserting every `.tsx` in it appears in `SOURCES` would close it for every future story at once.

- source_spec: none
  summary: Story 1.4 part B — the organization's branding: a logo (upload, storage, cross-organization read refusal, neutral fallback) and the brand accent that tints the application shell and logo lockup only.
  evidence: Split from story 1.4 at step-01's multi-goal check, human-decided 2026-09-15. The identity and localization half needs no schema work at all — `organizations` already carries `name`, `short_name`, `description`, `address`, `contact_email`, `organization_type`, `timezone`, `locale` and `leave_year_start_month`/`_day` from migration `0002` — so it is a surface, an RLS update policy and the L8 timezone wiring. Branding is a second top-level deliverable that introduces a subsystem the repository does not have in any form: no Supabase Storage bucket exists, `supabase/config.toml` and every migration mention storage nowhere, and both a logo reference and an accent colour are new columns. Each half merges without breaking the other; the settings surface is the natural host for the upload control, which is why it goes first. Covers epics.md story 1.4 acceptance clauses 3 and 4 (Q4's cross-organization asset refusal, the neutral fallback, UX-DR5's accent scope, and UX-DR4's proof that `destructive` stays reserved for an unresolved conflict even when the brand colour is the pilot fire department's red). Precedent for the split: story 1.1 measured 3005 tokens against a 1600 ceiling and split twice, 1.3 split once, and the navigation shell split once.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: A successful save is indistinguishable from one that silently matched no row — there is no confirmation string, no toast and no `aria-live` announcement.
  evidence: The submit handler clears the failure state and refetches; the screen looks identical before and after. This is the first surface in the application that writes anything, and the story goes to considerable length elsewhere to detect the "zero rows and no error" refusal shape — yet to the eye a refused save and a successful one differ only in whether an alert appears. Needs a sanctioned Croatian string and a decision on the announcement mechanism, so it is a UX decision rather than a patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: The write is hand-rolled rather than using `useMutation`, on the story whose stated purpose is to set the data pattern every later surface copies.
  evidence: The read uses TanStack Query; the write uses a `useRef` re-entrancy guard plus a `useState` pending flag plus try/catch/finally. `useMutation` supplies `isPending`, call serialization and `onSuccess` invalidation directly. The spec's Design Notes argue Query lands here "so whatever shape it takes becomes the pattern every later surface copies", and story 1.5's member list will copy whichever shape is present. Deferred rather than patched because changing it touches the in-flight assertions being added in this same review pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: Two admins saving concurrently silently clobber each other; there is no dirty check, no precondition and no `updated_at` column to compare against.
  evidence: Every save PATCHes all five editable columns regardless of which were edited, so the second writer overwrites the first with no signal to either. The table has no `updated_at`, so the fix requires a migration and a decision about optimistic concurrency. Last-write-wins may well be acceptable for a settings row edited by a handful of admins — but it is currently unstated rather than decided.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: Four columns are selected into the snapshot for no reader, in a module that excludes `created_at` on exactly that ground.
  evidence: `ORGANIZATION_COLUMNS` pulls `short_name`, `description`, `address` and `contact_email`; no surface renders them and the edits type cannot write them. The adjacent comment excludes `created_at` because "selecting it would be a column read for no reader" — the same test fails for these four. They are also the fields story 1.4's narrative mentions but no acceptance criterion names, so whether they gain controls is the open half of the same question.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: Native browser validation emits non-Croatian text on a codebase that polices every authored string by test.
  evidence: `required`, `min`, `max` and `type="number"` without `noValidate` produce validation bubbles in the browser's or OS's locale. `localization-applied.test.ts` and `resource-hygiene.test.ts` check every authored string to the byte, and this is the one class of user-facing text that walks past both. Adding `noValidate` means re-homing those refusals into the application's own message set, which is a design change rather than a patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: The leave-year month is a raw 1–12 number field in an application that already formats Croatian month names.
  evidence: `formatMonthName` exists in `format.ts` and returns CLDR month names; the label reads "Mjesec početka godišnjeg odmora" and the control shows `4`. A `<select>` of month names is more legible and removes the out-of-range case entirely — the same argument the day field already makes with `max={28}`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: A refused value is never attributed to the field that caused it — `organization.error.invalid` names nothing and no input carries `aria-invalid`.
  evidence: "Unesena vrijednost nije dopuštena." tells the person a value was rejected without saying which of five fields. UX-DR34 is cited throughout this change as requiring the refusal to name the problem, and the blank-name code honours it while the general check-violation code does not — although PostgREST returns the constraint name, from which the column is derivable exactly as `organizations_name_check` already is. Focus is also never moved to the message, so on a five-field form the `role="alert"` announcement can refer to a control scrolled out of view.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: The loading skeleton is a single 44px bar standing in for a five-field form, with no `aria-busy` and no accessible loading text.
  evidence: It neither reserves the form's layout — guaranteeing a jump when the snapshot arrives — nor tells a screen-reader user that anything is in flight. UX-DR40 requires a skeleton rather than a spinner, which this satisfies in kind but not in shape.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: The screen assembles its own data access at two call sites, so `ORGANIZATION_TABLE` has to be part of the snapshot module's public surface.
  evidence: `supabaseClient().from(ORGANIZATION_TABLE)` appears in both the query function and the submit handler. `snapshot.ts` goes to real trouble to make the table an injected parameter so the module is stubbable, then exports the table name so the screen can build the client itself. An `organizationTable()` helper would keep the seam in one place and let the constant stop being exported.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: The new `headingKey` detector reintroduces the `[^>]*` truncation trap the spec's own Code Map flags as a gotcha with teeth.
  evidence: `/<h1\b[^>]*>\s*\{t\('([^']+)'\)\}/` stops at the first `>`, so an `<h1>` carrying an arrow function or a generic in an attribute returns `null` and the destination-binding assertion fails on correct code. No current `<h1>` has that shape, so nothing is broken today — but this is the same failure mode as `buttonElements` at `prijava.test.ts:379`, reintroduced in a helper added to fix a different brittleness.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: `variant` was added to the global `STRUCTURAL_ATTRIBUTES` allowlist to serve one button, widening the exemption beyond its justification.
  evidence: The entry is argued honestly and proved on both polarities, but the allowlist is consulted for every screen in the sweep, so it also exempts `variant` on any future component where the prop is not drawn from `buttonVariants`' closed set. Scoping the exemption to `<Button>` would keep the widening as narrow as the argument that earned it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: `organizationTimeZone` has no consumer in application code, so the L8 acceptance criterion is proved only by its own unit test.
  evidence: A repo-wide grep returns only the definition and `snapshot.test.ts`; no `.tsx` in `apps/web/src` imports from `@/i18n/format`, because no shipped surface renders a date or a time yet. The ledger entry this story answered ("1.4 supplies the value, tests pass a literal") is therefore half closed: the value now has a source and still has no consumer. The first surface that renders a date must read the zone from the organization snapshot rather than passing a literal, and that is where the criterion becomes observable.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: The only executable proof of the new UPDATE policy is skippable, so whether it is verified at all depends on the operator's environment rather than on `pnpm test`.
  evidence: Every new case in `rls-isolation.test.ts` is `it.skipIf(noDatabase)` or `it.skipIf(noApi)`, and the repository still has no CI configuration. In a checkout with no reachable local Supabase, all of them skip silently and the suite reports green, leaving only comment-stripped source-text regexes in `supabase-scaffold.test.ts` as evidence that a policy guarding every credential in a tenant exists. This matches the repository's existing convention rather than being introduced here, and it compounds the already-recorded CI gap — but it now guards a higher-consequence invariant than when that entry was written.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4a-organization-settings.md`
  summary: There is no retry affordance on the read-failure path, although the message tells the person to try again.
  evidence: `organization.error.unavailable` reads "Pokušaj ponovno.", but on a failed read there is no form and therefore no button, and `useQuery`'s `refetch` is never called. Once the refusal is made visible (patched in this review pass), nothing on the screen can still act on it short of a browser reload.

- source_spec: none
  summary: Story 1.4 part C — the organization's brand accent: an accent value on the organization, a brand accent token, and the tint that reaches the application shell and the logo lockup and stops there.
  evidence: Split from story 1.4 part B at step-01's multi-goal check, human-decided 2026-09-16. Part B is the logo (storage bucket, upload, cross-organization read refusal, neutral fallback); the accent is a second top-level deliverable that merges without it and carries its own mechanism decision — the theme layer defines no brand accent token today (`apps/web/src/index.css` has only shadcn's inherited neutral `--accent` and `--sidebar-accent` hover tokens, lines 92-93 and 106-107, in both themes), so part C either authors one under the same measured-contrast rules the other 51 tokens follow or resolves the accent at runtime from organization data, which is a different mechanism and needs deciding rather than assuming. Ordered after the logo deliberately: UX-DR5 scopes the tint to the application shell and the logo lockup only, and neither exists yet — the navigation chrome is the navigation shell's part B, itself still unspecced on this ledger, and no lockup component exists anywhere in `apps/web/src`. Authoring the token first would tint almost nothing and be re-derived when the chrome lands. Covers epics.md story 1.4 acceptance clause 4 and UX-DR4's proof that a shift fill, a modifier and `destructive` are all untouched by the brand colour even when the pilot fire department's accent is red. Consequence for sprint-status: the `1-4` parent no longer completes with part B — it now closes with part C, and each part earns its own key when specced, same convention as the 1.1 and 1.3 parts.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4b-organization-logo.md`
  summary: The only executed proof of the storage policies is optional — every case is `it.skipIf(noDatabase)` / `it.skipIf(noApi)` — so in a checkout with no reachable local Supabase the first cross-tenant asset boundary in the system reports green while asserting nothing.
  evidence: Pre-existing convention rather than something this story introduced: `deferred-work.md` already records it for 1.3a and 1.4a, and there is still no CI configuration in the repository. What changes here is the stakes. The always-running counterpart, `test/supabase-scaffold.test.ts`, greps the migration text, so it proves the SQL says the right thing and never that the database does it: if `auth.jwt() ->> 'organization_id'` or the shape of `public.current_member_access()` ever drifts so the folder comparison stops matching, every admin's upload is refused 403, the feature is dead for every tenant, the migration text is byte-identical and the executing cases skip. The reviewer that found this also demonstrated the reverse direction — a bucket that ends up `public` at apply time is caught only by a skipped case. Closing it needs a runner with a stack attached, which is the CI entry already on this ledger.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4b-organization-logo.md`
  summary: `expectedControls` in `apps/web/src/routes/prijava.test.ts` sums `<Input>` and `<Button>` occurrences into one number, so it cannot notice the substitution its own comment claims it catches.
  evidence: The comment says the number is what notices a sixth field, but because the count is a single total, a sixth field arriving in the same change as a removed button leaves the total unchanged and the assertion passes. The 44px sweep still measures whatever controls it finds, so this is a weakened tripwire rather than an unguarded surface. Splitting the expectation into a field count and a button count restores the claim the comment makes, and would do so for every screen in `SCREENS` at once rather than only for the settings surface.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-b-chrome-and-sign-out.md`
  summary: `/` is the one signed-in screen with no chrome — no navigation and no exit — and this story's new guards are what send people there.
  evidence: `SignedInScreen` is registered on `rootRoute` outside `appLayoutRoute` (`router.ts:42-55`), deliberately, and `router.test.ts:89-103` pins the route-id list so moving it under the layout changes `/` to a nested id and is a decision about its match chain rather than a refactor. The hole predates part B — `/` has rendered a bare heading since part A — but part B makes it reachable by a new path: both sign-in routes now `throw redirect({ to: '/' })` for an already-signed-in visitor, so the screen with no way out is now where the application deposits anyone who revisits a sign-in URL with a live session. `router.test.ts:602` describes `/` as "the one path that decides where a signed-in person belongs", and today it decides nothing. Resolve by giving `/` a destination-redirect or by bringing it inside the layout; both are route-tree decisions with tests pinned to the current shape.

- source_spec: `_bmad-output/implementation-artifacts/spec-navigation-shell-b-chrome-and-sign-out.md`
  summary: On a phone an admin's later destinations scroll horizontally out of view with no indicator, no overflow affordance, and no guarantee the current destination is in view.
  evidence: The bottom bar is `overflow-x-auto` with `shrink-0` entries because nine 44px targets plus the exit do not fit across a phone — a deliberate choice recorded in the code, and the right one for keeping the PAGE from scrolling sideways. What it leaves open is discovery: nothing signals that more destinations exist past the right edge, nothing scrolls the active entry into view on load, and the exit sits last so it is the most likely to be off-screen for exactly the admin role that has the most destinations. The member role reaches four and is unaffected, so this is admin-on-a-phone only. The fix is a design question — an overflow menu, a scroll affordance, or a different mobile treatment for the admin set — rather than a patch, and no artifact in `ux-designs/` covers it.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4c-brand-accent-and-lockup.md`
  summary: `invalidateQueries` rejecting after a successful write reports that write as a refusal, in `submit` and `uploadLogo` on the settings surface.
  evidence: Part C fixed this on the accent path only, because that was the path in its diff. The identical shape sits in the two handlers 1.4a and 1.4b shipped: the invalidation is inside the same `try` as the write, so a rejection there sets the failure state for a write the database already accepted, and the person is told their save was refused while the row holds the new value. It is a narrow window — an invalidation rejects only if the query client is torn down or a queryFn throws synchronously during the refetch — which is why it has survived two reviews, but the accent path now behaves differently from its two neighbours on the same screen, and that inconsistency is the more likely source of a future misreading than the bug itself.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4c-brand-accent-and-lockup.md`
  summary: DESIGN.md's `colors:` front matter does not describe the eight brand accent tokens, so the design source of truth no longer covers what ships and `theme-fidelity.test.ts` structurally cannot see them.
  evidence: `test/theme-css.ts` records the absence deliberately rather than by oversight — the accents were authored against the measured-contrast rules in `theme-contrast.test.ts`, not transcribed from DESIGN.md, because no design artifact names a brand accent at all (the same gap `deferred-work.md:211` recorded when the accent had no token). The consequence is that `theme-fidelity.test.ts`, which exists to catch a token whose value drifts from its documented source, has nothing to compare these eight against — they are measured but not described. Closing it means adding them to DESIGN.md's front matter and extending the fidelity sweep, which is a documentation decision about what DESIGN.md is for now that a colour can be authored test-first.

- source_spec: none
  summary: Story 1.5's write path — implement `createUser` and `updateUserById` in the privileged auth function, plus the member create/edit form, the no-email identity, and the admin-issued password reset.
  evidence: Split from story 1.5 at step-01's multi-goal check (human decision 2026-09-17), which carries two top-level independently shippable deliverables: the member list at scale (built now) and this write path. Each is a separate PR without breaking the other — the list ships over the seeded fixtures because `members_select_own_organization` already exists from 1.3a, and this half is provable without a browser because the epic's test discipline puts the boundary and RLS assertions in `test/`. The coupling is one-directional and decided the order: the form needs a surface and a rendered row to confirm against, so building it first would render list-shaped markup that belongs to the list story and would be reviewed there. Deferring it also keeps its review purely about the secret-key boundary — `supabase/functions/admin-auth/handler.ts:195` still answers `501 NOT_IMPLEMENTED` for all four declared operations, so this is the last unbuilt piece of the AD-16/AD-17 privileged boundary, and epic 1's cross-story dependencies note that story 1.6's deactivated-user sign-in block routes through the same function — build it once, here.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5a-member-list.md`
  summary: An organization larger than PostgREST's `max_rows` ceiling cannot be listed completely — 1.5a detects the truncation and refuses, but nothing paginates past it.
  evidence: `supabase/config.toml:15` sets `max_rows = 1000`. Q20 asks for "several hundred", so the ceiling sits above the requirement and 1.5a's answer is to fail closed rather than present a short list as a complete one. That is correct for this story and useless to an organization that actually has 1200 members: the screen would refuse rather than degrade. Closing it means a real paging or windowing strategy (range headers, cursor, or virtualization) and a decision about whether the stated count means "loaded" or "exists", neither of which belongs in a story whose scale clause stops at several hundred.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5a-member-list.md`
  summary: The native `<select>` control's ~200-character class string is now duplicated across two screens with no shared primitive behind it.
  evidence: `apps/web/src/routes/organizacija.tsx:701` and the member list's permission-level filter carry byte-identical class strings. shadcn ships a `Select`, and `DESIGN.md:117` lists it among the components inherited wholesale, but neither story vendored it — 1.4a used a native element to keep the uncontrolled-field pattern intact, and 1.5a followed the precedent rather than introducing a primitive mid-story. The next change to focus ring, height or disabled styling has to find both copies, and a third screen makes it three. Vendoring `select.tsx` is the fix, and it should happen in a story that renders one rather than as a drive-by.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5a-member-list.md`
  summary: UX-DR17 and UX-DR19 ask the member list to filter by team and by member; only the permission-level axis exists until teams do.
  evidence: `members` carries no `team_id` — team membership is versioned into its own table by story 1.7 (AD-2). 1.5a therefore builds the filter over the only axis the schema offers, and satisfies ONE of UX-DR19's three clauses: the count is in the label. The options are hard-coded from `MEMBER_ROLES` rather than populated from live records (the rule says "never hard-coded"), and there is no one-action reset for search plus level anywhere on the screen — an earlier draft of this entry claimed all three, which was wrong. When 1.7 lands, the team axis joins the same control rather than replacing it, and the roster's "names and membership only" rule (story 1.8) has to be reconciled with what this screen already shows an admin.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5a-member-list.md`
  summary: Three other ADMIN_ONLY destinations carry no route guard, and `/organizacija` — a built settings surface with write actions — is still reachable by a member-role session typing the URL.
  evidence: `navigation/destinations.ts:105-108` already marks `/raspored`, `/ljudi`, `/postavke-rotacije` and `/organizacija` as `ADMIN_ONLY`, but 1.5a guards exactly one of them and does so with a hand-written check rather than deriving the decision from `destination.roles`. So the role policy is now stated twice and the two can drift. `/organizacija` matters most: unlike the placeholders it holds real data and real writes, and while RLS refuses the writes (AD-10), the screen still renders the organization's settings to anyone who types the path. The fix is one guard driven by the destination table, applied to every `ADMIN_ONLY` entry, which cannot disagree with what the navigation renders; doing it here would have meant 1.5a silently changing three other destinations' behaviour.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5a-member-list.md`
  summary: The member list's search, sort and filter live only in component state, so a narrowed list cannot be linked, bookmarked, or recovered after Back or a refresh.
  evidence: TanStack Router already types search params for this application, and AD-14 has the host answer every path with `index.html`, so `/ljudi?q=maric&razina=admin` is a shape a real link could take. Today an admin who narrows several hundred members to the three they care about cannot hand that view to anyone, and navigating away loses it. This was not in 1.5a's scope — the story's clauses are search, sort, filter and scale, none of which mention addressability — but the screen is where the decision would land, and it interacts with the sort state the URL would also need to carry.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5a-member-list.md`
  summary: `/ljudi`'s route guard awaits a role read with no time budget, so a request that never settles leaves the visitor on the previous screen with no explanation.
  evidence: `beforeLoad` awaits `context.currentMemberRole()`, which reaches the network. Its rejection path is handled and tested, but a promise that neither resolves nor rejects — a hung connection, a captive portal — is not a rejection: TanStack Router simply never completes the navigation, so pressing `Ljudi` appears to do nothing at all. Every other guard in the tree has the same shape, so this is a tree-wide decision about a navigation budget rather than a defect in this route, and picking a timeout means deciding what the visitor sees when it fires.

- source_spec: none
  summary: The admin-issued password reset — an admin sets a new password on an existing member through the privileged auth function, the only reset path an account with no email address can have.
  evidence: Split from story 1.5's write path at step-01's multi-goal check (human decision 2026-09-18), which carries two top-level independently shippable deliverables: the create/edit write path (built now) and this reset. The write path merges without it and is complete as "an admin creates and edits members"; the reset then merges as a password field on an `updateUserById` call that already exists, so nothing is built here and left unexposed. It was deferred rather than kept because the combined spec measured well past the 900–1600 standard before the reset was counted — two real operations on the secret-key boundary, the database-side authorization AD-16 requires, the form, the i18n keys, three gate registrations and the RLS and boundary assertions — and story 1.5a's `bad_spec` LOOPBACK one day earlier is recent evidence that this epic punishes an overloaded spec rather than absorbing it. Consequence for epics.md: story 1.5 acceptance clause 1 is split across the two parts — the write path covers "the account is usable" with no email address, the privileged function's secret-key/JWT division and its database-verified refusal, while "its password reset is an admin-issued action rather than a self-service email" (CAP-1, AD-12) is covered only when this lands. Consequence for sprint-status: the `1-5` parent does NOT close with the write path — it closes with this reset, and each part earns its own key when specced, same convention as the 1.1, 1.3 and 1.4 parts. It is also the second half of the account-lifecycle question story 1.6 reopens through `ban`/`unban` on the same function, so whichever lands first sets the shape for the refusal codes and the confirmation step the other copies.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5b-member-write-path.md`
  summary: A half-completed member write leaves a state only an operator can settle, and there is no in-product tooling for either — the admin is told to find one.
  evidence: `createUser` deletes the auth account when the `members` insert is refused, and `updateUserById` restores the row when the address will not move; when the compensation ITSELF fails the boundary answers `ACCOUNT_NOT_REMOVED` or `USERNAME_NOT_RESTORED`, which are honest but terminal. The first strands an `auth.users` row holding the synthesized address, so that username can never be issued again — GoTrue answers `email_exists` forever — and the second leaves `members.username` and `auth.users.email` disagreeing, which means the person signs in at one name while the list shows another. `DEPLOY.md` now carries SQL procedures for both, so an operator CAN settle them, but an admin cannot, and nothing surfaces the state anywhere except the one refusal message on the screen that produced it. Closing it means either a genuinely atomic path (which the `auth` boundary does not offer) or an operator surface listing accounts with no member row and members whose username disagrees with their address — a decision about who owns that surface, since the SPA ships the publishable key only and the privileged function holds no read operation today.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5b-member-write-path.md`
  summary: The rename decision is made against a cached member row, so two admins editing the same person concurrently can skip a rename that was needed.
  evidence: `usernameChanged(member.username, edits.username)` compares what the form holds against the row the list read, and the list is cached under `MEMBERS_LIST_KEY` with a stale time. If another admin renames the same member between this screen's read and its save, the comparison is against a username the database no longer holds: an edit that should move the identity decides it has nothing to move, the PostgREST half writes the other four columns, and `members.username` and `auth.users.email` stay as the other admin left them — no refusal, because nothing was refused. The window is small and the consequence is not corruption but a silently skipped rename. Closing it means the function deciding the comparison against the row it reads under the caller's JWT rather than the client deciding from cache, which changes `updateUserById`'s payload contract and belongs with whichever story first has two admins editing concurrently as a real scenario. Related to the READ COMMITTED entry above, which this story's routine member writes also make more reachable.
